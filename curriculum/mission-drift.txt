# Units whose lesson does not yet quote its mission_link verbatim (gate 8).
#
# Read scripts/mission.py's docstring first: this is a RATCHET, not a
# suppression list. A unit listed here is excused from failing; a unit listed
# here that starts PASSING fails the run until it is struck off; and a unit
# ADDED since the base ref fails the run. The list can only shrink.
#
# THIS FILE IS PERMANENT. When the last entry goes, empty it — keep this header
# and delete the ids. Do NOT delete the file. "The base ref has no list" is the
# single condition under which the growth check stands down, because it means
# the commit that introduced the list; if the file can disappear, that
# condition stops being unique, and a later branch could reintroduce the file
# alongside fresh mismatches and have them excused. mission.py refuses a
# deletion for that reason.
#
# Established 2026-08-21, the first time gate 8 was run over the whole
# committed corpus rather than over the lesson in front of an author. It failed
# 15 of 81. The S2 plan had recorded three (aa-00, aa-01, pw-03) — that was the
# sample, and this is the population.
#
# The 15 fall into two classes, and the class decides who repairs them.
#
# --- Class A: content divergence (1 remaining of 4) -------------------------------------
# The lesson asserts something the syllabus does not. aa-00's syllabus strip
# calls the unit an optional on-ramp about abstracting structure; its lesson
# claims groups measure symmetry as homology measures holes. These are two
# different claims and only the syllabus's is quotable. LESSON-GUIDE's rule
# applies: quote the strip verbatim and refute it in the body if it is wrong;
# never soften the quotation to make it true.
#
# aa-00, aa-01 and pw-03 were S1 units already slated for re-authoring (they
# predate every S2 convention); they come off this list as those units are
# repaired. tda2-02 is S4 and needs its own pass.
#
# pw-03 STRUCK 2026-08-22 on the s1-pw branch: its lesson now quotes the
# syllabus sentence exactly and carries the flourish in the paragraph below.
# The ratchet reported it as STALE before it was struck, which is the stale
# rule doing its job on a real repair rather than on a control.
#
# aa-00 and aa-01 STRUCK 2026-08-23 on the s1-aa branch. aa-00 had been
# carrying a sentence about groups and homology that belongs to no unit's
# syllabus entry; aa-01 quoted the right sentence but promoted its Z to a
# blackboard bold and added a clause about integer coefficients. Both now
# quote the syllabus character for character. The ratchet reported both as
# STALE before they were struck -- in the pytest suite rather than at the
# command line, which is where the binding between this list and reality
# actually lives.
tda2-02
#
# --- Class B: typography reverted while quoting (11) ---------------------
# The lesson quotes the right sentence but re-renders the syllabus's ASCII
# maths as Unicode while doing it: H^n -> Hn, H_{n-1} -> Hn−1, Z -> ℤ,
# F_p -> 𝔽p, S^2 x S^1 -> S2 × S1, A_n -> An, Kunneth -> Künneth, and in
# tda2-06 straight quotes -> curly ones. It reads as house style — lessons use
# literal Unicode maths and never LaTeX — but the mission strip is the one
# paragraph in a lesson that is a quotation rather than prose, and prettifying
# a quotation is editing it.
#
# That this is a defect and not the convention is settled by the corpus, not by
# argument: at1-03, at1-04, at1-06, at1-09 and tda2-10 all carry ASCII maths in
# their strips (H_1, pi_1, F_2, H^0), quote it exactly, and pass. The authors
# who reverted it are the deviation.
#
# All 11 are S3/S4 content. The repair is mechanical — replace the strip's text
# with the syllabus's, character for character — but it is 11 lessons in two
# semesters this branch is not otherwise touching, so it is its own PR.
at2-01
at2-02
at2-03
at2-04
at2-05
at2-06
at2-07
at2-08
tda2-01
tda2-03
tda2-06
