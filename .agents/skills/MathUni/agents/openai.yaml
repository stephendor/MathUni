interface:
  display_name: "MathUni"
  short_description: "Repo-specific patterns and workflows for MathUni"
  default_prompt: "Use the MathUni repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true