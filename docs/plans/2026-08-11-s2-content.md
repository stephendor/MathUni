# S2 content build — plan and tracker

**Goal.** Every S2 unit gets its two content artifacts: a problem set
(`problems/sets/<unit>.md`) and a lesson (`lessons/<module>/<unit>.html`), each
passing the eight gates below and the rubric in `curriculum/LESSON-RUBRIC.md`.

**Scope.** 31 units — `top` (13), `an2` (9), `lab` (9). At start: **0 problem
sets, 0 lessons** for all three modules; `lessons/top`, `lessons/an2` and
`lessons/lab` do not exist. Nothing is inherited; every artifact is new. Every
S2 unit has a `mission_link` in `syllabus.yaml`; none is missing.

**Branch.** `s2-content`, cut from `main` at `17b7101` on 2026-08-11, after
PR #16 (the tda1 module) merged. All of S1's shipped units, S3 and S4 are
therefore present and may be cited. Content-only: **no syllabus edits, no gate
edits.** Resource problems are recorded under Open decisions and handed to a
separate syllabus pass — S2 generates more of these than any previous semester.

**Order.** `top → an2 → lab`.

`top` first is forced by both dependency and evidence. It is the only S2 module
whose sources needed no adjudication at all — Munkres and Hatcher are both in
`bookmap.json` with per-page markdown — and four already-shipped S3 units cite
`top` units by number. `an2` second: its sources were missing at the start of
this session and are now settled (see Open decisions), and five S3 units depend
on it. `lab` last, because it is the module whose *shape* is new — its artifact
is code rather than proof, and neither LESSON-GUIDE nor LESSON-RUBRIC has a line
about that.

---

## S2 is written last, and its downstream units already specify it

This is the one thing S2 has that no earlier semester did. `at1`, `at2`, `tda1`,
`tda2` and `cap` were all authored **before** their S2 prerequisites and make
specific, already-shipped claims about what S2 teaches. These are a contract, not
a courtesy: **read the citing lesson and problem set in place before writing the
unit it names.** Where a promise turns out to be unsupportable from the source,
say so under Open decisions and flag it for the syllabus pass — do not quietly
write around it.

| S2 unit | Promised by | What was promised |
|---|---|---|
| `top-04` | `at1-01` lesson + set | continuity and homeomorphism; specifically that a homotopy is one **jointly** continuous map on `X × I`, not a family of continuous maps |
| `top-05` | `at2-05` | the product topology, as the input to Künneth |
| `top-12` | `tda2-06` lesson + set | the quotient topology; the Reeb graph is stated to be a quotient "in the sense of top-12" |
| `top-13` | `at1-01`, `at1-02` | quotient constructions, and the universal property "a map out of a quotient is continuous exactly when its composite with the quotient map is" |
| `an2-01` | `tda1-05` (five places) | the metric space axioms **as a checklist** — tda1-05 Problem 5 has the reader run them, and tda1-05 established that *neither* of its own sources defines a metric on diagrams |
| `an2-03` | `tda1-06` | compactness: sequential, total boundedness, Arzelà–Ascoli |
| `an2-05` | `tda1-07` | uniform convergence of derivatives and integrals |
| `lab-03` | `tda1-09`, `tda2-08` | Ripser and boundary-matrix reduction; tda2-08 contrasts "lab-03 finishing" with "lab-03 exhausting memory" |
| `lab-04` | `tda1-05` | "lab-04 computed these, here they are defined" |
| `lab-06` | `tda2-09` | landscapes, images, kernels |
| `lab-08` | `tda2-07` | Mapper — tda2-07 opens "In lab-08 you ran Mapper and it drew you a graph" |
| `lab-09` | `cap-02` | a full analysis, whose parameter choices cap-02 then audits |

**S1 is largely unwritten.** Only `aa-00/01`, `an-01/02/03`, `la-01/02` and
`pw-01/02/03` exist. S2 units name `an-05 … an-14`, `la-05/06/07/15`, `aa-18/20`
and `pw-04` as prereqs and none of those lessons exist. **Cite them as syllabus
units, never as lessons the reader has read.**

---

## Per-unit procedure

1. **Extract.** Pull the sections named in the unit's `resources` from the
   per-page markdown (`scratchpad/pull.py <Book> <pdfA> <pdfB> [out.md]`) and
   read them. Never write from recollection (LESSON-GUIDE §Source discipline).
   **Cite printed page numbers, and read the folio off the page you cite** —
   verify the offset yourself rather than assuming one. Record in the source map
   below **how the extract was actually taken**, filled in after the extraction,
   never in advance.
2. **Problem set** `problems/sets/<unit>.md`: 5 problems, easy→hard, each with
   `Nudge` / `Strategy` / `Partial` / `Worked start` `<details>` blocks; every
   cited theorem/definition id verified present in the extract. Interleave
   earlier units where the prereqs invite it. **LaTeX is the convention in
   problem sets.**
3. **Lesson** `lessons/<module>/<unit>.html` from `lessons/_template.html`,
   following LESSON-GUIDE §Structure. Read the problem set *first* — every
   technique it uses must be demonstrated by a worked or faded example and every
   id it names must appear (gate 1). **Literal Unicode maths in lessons, never
   LaTeX.**
4. **Gates** — all eight (below).
5. **Source read-back** against the extract — a manual check, and *not* gate 7,
   which is the canvas render-back below. Every definition, theorem statement,
   proof step, worked answer, `data-ok` and model answer, checked against the
   page it cites, **and against what the page *does* with the material** —
   proves, states, sets as an exercise, disclaims. This is where the real defects
   are; in S3 it caught the persistence algorithm attributed to the wrong section
   and an unread mechanism asserted as fact.
6. **Commit by explicit path, updating this file's tracker row to ✓ in the same
   commit.**

### Record modality, not only location

Every **result** cited by a lesson carries a **modality** — `proves`, `states`,
`sets-as-exercise`, `disclaims`, `applies` — because a citation that verifies
where a result lives does not verify what the text does with it, and the lesson's
verb ("Munkres proves", "Lindström sets") is checkable only against a recorded
field. Three of this session's audit findings were modality errors, not location
errors; see Open decisions. This follows open observation
`2026-08-10-source-asserts-what-it-only-states`.

**Where the field is recorded, and where it is not.** The modality of a result is
recorded in the lesson footer that cites it, at the point of use. The table under
*Modality, where it has already been read and matters* is not the register — it
carries only those results whose modality was settled during the audit **and**
where getting it wrong would change a lesson's wording; it is deliberately
partial and will not grow to cover every citation. The offset and section-start
tables that precede it are **page-location tables with one row per book**, not
source-result rows, so modality does not apply to them and no column is missing.

---

## The eight gates

Gates 1–6 and 8 are executable; **gate 7 is the manual canvas render-back**, and
nothing else in this document is gate 7. The source read-back of step 5 above is
a second manual check with no gate number — it is the harder of the two and the
one that finds the mathematics defects, but numbering both would let one be
counted for the other. Run from the repo root, per unit:

```bash
python scripts/check_lesson_coverage.py problems/sets/<u>.md lessons/<m>/<u>.html
```

```bash
python scripts/lesson_lint.py lessons/<m>/<u>.html
```

```bash
python -c "from html.parser import HTMLParser; HTMLParser().feed(open('lessons/<m>/<u>.html',encoding='utf-8').read())"
```

```bash
python <scratchpad>/gate.py lessons/<m>/<u>.html
```

```bash
python <scratchpad>/mission.py lessons/<m>/<u>.html
```

Gates 1–3 are the built-ins: coverage, lint, HTML parses. Gates 4–6 are
`gate.py` — tag balance, no external requests, `node --check` on every inline
`<script>` — each of which caught a real S4 defect that the built-ins miss.
Gate 8 is `mission.py`. `gate.py`, `mission.py`, `pull.py` and `build.py` are
**not in the repo**; they were copied into this session's scratchpad from the S3
session's. Promoting them into `scripts/` has now been deferred three times and
is proposed under Open decisions.

### Negative controls — both re-run 2026-08-11, before any authoring

**gate.py — fires.** `negctl.html`, carrying a crossed `<sup>x</sub>`, an
`https://…cdn` link and a stray `;;)` inside an IIFE, returns three FAIL lines
and exit 1. The positive control (`lessons/tda1/tda1-09.html`) returns three PASS
lines and exit 0.

**mission.py — the inherited control was not a control, and has been replaced.**
`mission_negctl.html` exits 1, which the S3 plan recorded as evidence the gate
can fire. It is not. The script derives the unit id from the filename with
`([a-z0-9]+-\d+)\.html$`; `mission_negctl.html` does not match, so
`re.search(...)` returns `None` and the script raises `AttributeError` at line 7
**before the mission strip is ever compared**. Exit 1 came from a traceback, not
a verdict. A real control was built this session — a shipped lesson copied to
`negctl/tda1-09.html` with four words appended inside `<p class="mission">` — and
it returns `FAIL tda1-09 mission strip verbatim`, exit 1, printing both strings.
That is the watched failure; the positive control returns
`PASS tda1-09 mission strip verbatim`, exit 0. Logged as
`2026-08-11-mission-negctl-crashes-instead-of-firing`.

> **A non-zero exit is not a watched failure.** Accept a control only on the
> printed FAIL line naming the check. A crash, a missing file and a bad argument
> all exit 1 too.

### Gate 7 — the canvas actually renders

`node --check` proves a script *parses*, not that it *draws inside the viewport*.
`cap-04` shipped a diagram laid out at y = 334–368 inside a 340 px canvas —
clipped, silent, green on every other gate. So: compute node positions on paper
and check them against the canvas height before writing the script, then open the
lesson in the browser pane and read back the painted bounding box with a
`getImageData` alpha scan. **Leave real margin** — 7 px was too thin once and the
canvas had to be resized.

### Known gate defect — FIXED 2026-08-13, and it was not fixed by reflowing prose

`check_lesson_coverage.py`'s `REF_PATTERN` uses `\s+` between keyword and number,
and `\s+` eats newlines, so a citation wrapped across lines ("Theorem\n1.6")
produced an unmatchable ref and a false failure. It fired three times in S3 and a
fourth in `lab-05`, on `Corollary\n3.6`. Reflowing the prose makes it go away and
is **the wrong repair**: the citation was always well formed and it was the
*comparison* that was line-break sensitive.

**Repaired at the comparison.** `find_refs` now canonicalises each matched ref by
collapsing internal whitespace to a single space, and `find_missing_refs` searches
the lesson with `keyword\s+number` so the lesson's own wrapping cannot cause a
false miss either. Four tests were added — a wrapped citation canonicalises; a
wrapped set is covered by an unwrapped lesson; an unwrapped set is covered by a
wrapped lesson; and, the negative control, wrapping does **not** make an absent
reference look present. Verified non-regressive across every unit in the repo:
`top-02..13`, `an2-01..09` and `lab-01..05` all report the same reference counts
as before the change. Observation
`2026-08-10-coverage-gate-breaks-on-wrapped-citations` can be closed.

### What the gate suite does not do

Every executable gate answers "is this well formed?" and none answers "is this
right?" A full green board is seven instances of one kind of confirmation, not
seven independent ones (`2026-08-10-gates-check-syntax-never-sense`). Two further
consequences specific to S2:

- **The problem set receives no automated check of any kind.** The `<details>`
  blocks are graded content; a `Partial` hint that states a falsehood passes
  everything. Give them the same read-back as the lesson body.
- **`lab` will trip gate 1 into vacuity.** A problem set that cites library APIs
  rather than numbered results yields `UNCHECKED checked 0 refs`, which exits 0.
  Where a lab set does cite numbered results from Edelsbrunner or Oudot, run the
  gate with `--min-refs` set to the count actually cited so the denominator is
  enforced rather than merely reported.

### Gate 9 — `lab` outputs are re-derived, not attested

Added 2026-08-13, with the `lab` module, and the first gate in the suite that
answers "is this *right*?" rather than "is this well formed?" — because for
`lab` the two coincide: the claim a lab problem set makes is "this code was run,
in this environment, and produced this", and that is mechanically checkable.

```bash
LAB_PYTHON=<scratchpad>/labenv/Scripts/python.exe python scripts/check_lab_outputs.py problems/sets/lab-01.md
```

A lab set carries one ```` ```env ```` block of `name==version` pins and pairs
each ```` ```python id=<name> ```` block with a ```` ```text id=<name> ````
block. The gate re-executes the code blocks in order in one interpreter — they
share state, like a notebook — and checks three things:

1. **the outputs**, per block, against what was recorded;
2. **the environment**, by reading each pinned distribution's installed version
   out of the target interpreter through `importlib.metadata`;
3. **determinism**, by running the whole file twice and requiring agreement, so
   an unseeded computation cannot record an output that was true once.

Blocks with no `id=` are ignored, so a set can still show code the reader is
meant to complete. A set with executable blocks and no `env` block **fails**:
an unattributed output is the thing the gate exists to prevent.

**Negative controls — run 2026-08-13, both printing a FAIL line naming the
check, neither a crash.** Changing one recorded digit (`death 1.6339` →
`1.6338`) returns `FAIL circle output differs` with both strings and exit 1.
Re-verifying a correct set under the *repo's own* interpreter returns four FAIL
lines — `giotto-tda … is not installed`, `gudhi is pinned at 3.13.0 and the
interpreter has 3.11.0`, `python is pinned at 3.11.11 and the interpreter has
3.13.5`, and the block raising `ModuleNotFoundError` — which is precisely the
hazard this section already warned about, now caught mechanically instead of by
a note. `tests/test_check_lab_outputs.py` carries **47** tests, one per failure
mode, including a nondeterministic block and a set whose output block matches no
code block. (It said 29 and carried 28 — a self-attested count that no check
compared with the file, caught in review. The first PR-comment pass added the
missing duplicate-output-block-id control the miscount had concealed, plus
controls for conflicting pins, `env` module coverage, exceptions on a repeat run,
and `--parse-only`; the second added name-level import coverage and a control
watching the gate refuse to execute a set whose environment declaration has
already failed.) The suite as a whole is at **112**.

---

## Source page map

**Printed pages are what the corpus cites.** The per-page markdown carries the
printed folio in its first lines; quote that, never the `page-N` directory index.

### Offsets — verified 2026-08-11 by direct folio reads

**Sign convention: `printed = PDF + offset`**, so every offset below is negative
and the printed folio is always the smaller number — the front matter is what the
offset absorbs. Munkres: `20 + (−17) = 3`, matching the verified folio.

| Book | Offset (printed = PDF + offset) | Verification points | Extraction method | Units |
|---|---|---|---|---|
| Munkres | **−17** | PDF 20 = printed 3 · 90 = 73 · 100 = 83 · 105 = 88 | `pull.py` | all 13 `top`; substitute for `an2-01/02/03/08` |
| Hatcher | **−9** | §1.1 heading PDF 34 = printed 25, agreeing with S3's PDF 60 = 51 · 140 = 131 | `pull.py` | `top-13` |
| Lindström | **−13** | PDF 74 = printed 61 · 75 = 62 · 98 = 85 · 120 = 107 · 122 = 109 · 187 = 174 · 188 = 175 | `pull.py` | all 9 `an2` |
| Abbott | **−12** | PDF 166 = printed 154 · 234 = 222 | `pull.py` | `an2-01/05/06` |
| Edelsbrunner | **−12** (S3; re-verify at `lab`) | S3: PDF 73 = printed 61 · 105 = 93 · 190 = 178 | `pull.py` | `lab-02/03` |
| Oudot | **−9** (S3; re-verify at `lab`) | S3: PDF 38 = printed 29 · 58 = 49 | `pull.py` | `lab-04/05` |

> **Treat every offset as provisional and read the folio off the page you cite.**
> S4's plan asserted a constant Ghrist offset that was false — it *decreased*
> through the book, via unnumbered chapter-opener pages. A page with no folio
> proves nothing about the offset. The section-start tables below are for
> **locating**, never for citing.

> **Abbott carries its Instructor's Solutions Manual bound in.** The textbook
> ends at PDF 269 (index); **PDF 270 opens "Understanding Analysis — Instructor's
> Solutions Manual"** and repeats every section heading from 1.1 onward. A naive
> heading search therefore finds each section twice, and the second hit is the
> solutions. All Abbott citations below are from the first occurrence. Anything
> at PDF ≥ 270 is the solutions manual and is not the text.

### Section starts — printed pages, located by heading and folio-checked

**Munkres** (`top`; also the substitute for `an2-01/02/03/08`) — §12 **75** ·
§13 **78** · §14 **84** · §15 **86** · §16 **88** · §17 **92** · §18 **102** ·
§19 **112** · §20 **119** · §21 **129** · §22 **136** · §23 **148** · §24 **153** ·
§25 **159** · §26 **163** · §27 **172** · §28 **178** · §29 **182** · §30 **190** ·
§31 **195** · §32 **200** · §43 **264** · §45 **275** · §46 **281** · §47 **290**.

**Hatcher** (`top-13`) — ch. 0 opens printed 1; the CW-complex subsection from
printed 5.

**Lindström** (`an2`) — 3.1 **43** · 3.2 **48** · 3.3 **52** · 3.4 **59** ·
3.5 **63** · 3.6 **68** · 3.7 **71** · 4.1 **79** · 4.2 **81** · 4.3 **86** ·
4.4 **92** · 4.5 **99** · 4.6 **101** · 4.7 **103** · 4.8 **107** · 4.9 **112** ·
4.10 **116** · 4.11 **123** · 5.1 **133** · 5.4 **150** · 6.1 **174** ·
6.2 **182** · 6.6 **201**. Chapters: 3 Metric Spaces **43** · 4 Spaces of
Continuous Functions **79** · 5 Normed Spaces and Linear Operators **133** ·
6 Differential Calculus in Normed Spaces **173**. **A rule recorded at `an2-07`
and withdrawn at `an2-09`.** Chapters 3, 4 and 5 open on the same folio as their
first section — 43, 79 and 133 — because the opener page carries the chapter
title, the section heading and the section's first text. **Chapter 6 does not**:
its opener at printed 173 carries a full page of introduction and no section
heading, and §6.1 begins on printed 174. Three instances were still not a rule.
The only reliable procedure is the one stated above the table: read the folio off
the page you cite.

> **Two corrections from the `an2-01` extraction, both found by reading folios.**
> Chapter 3 opens on printed **43**, not 44: the chapter-opener page (PDF 56)
> carries the "Chapter 3 / Metric Spaces" title, the §3.1 heading **and** the
> first paragraphs of §3.1, with the folio 43 at the foot. The earlier table had
> §3.1 at 43 and the chapter at 44, which cannot both be right. Second,
> **Definition 3.1.1 — the metric axioms, and the single most-cited item in
> `an2` — is on printed 44, not 43**: §3.1 begins on 43 and the definition falls
> after the page break. Counting forward from a section heading would have put it
> on 43 both times. Verified: PDF 57 → folio 44, PDF 58 → folio 45.

**Abbott** (`an2-01/05/06`) — 6.2 **154** · 6.3 **164** · 6.4 **167** ·
6.5 **169** · 6.6 **176** · 6.7 **181** · 8.2 **222** · 8.3 **228**. The
Weierstrass M-test is at printed **168** (in §6.4); the Weierstrass
Approximation Theorem is at printed **242** (in §8.3) — see Open decisions.

### Modality, where it has already been read and matters

| Source | Result | Modality |
|---|---|---|
| Abbott §8.2, printed 222 | the metric axioms (Definition 8.2.1) | **states**, then **sets-as-exercise** — Exercises 8.2.1–8.2.3 have the reader run the checklist. Exactly what `tda1-05` promised `an2-01` would be. |
| Abbott §8.3, printed 242 | Weierstrass Approximation Theorem | **sets-as-exercise** (the Fejér-kernel sequence, Exercise 8.3.10) |
| Lindström §4.2, printed 85 **and §4.3, printed 89** | Weierstrass' M-test | **both** — **sets-as-exercise** as Exercise 11 to §4.2, named in the exercise text; and **proves** it as **Proposition 4.3.4** at printed 89. Corrected during the `an2-05` extraction; the earlier row recorded only the exercise and that omission drove an `an2-06` decision (see Open decisions). |
| Lindström §3.4, printed 61–62 | contraction, and the fixed-point theorem | **proves**, with the rate-of-convergence estimate (3.4.1) |
| Lindström §4.6, printed 102 | **Theorem 4.6.4** — C(X, Y) complete for X compact, Y complete | **states**. No proof and no exercise; "The following theorem sums up the results above for X compact." **Cited as a completeness supplier by six lessons — an2-02, an2-03, an2-04, an2-05, an2-06, an2-07 — each recording it as "an2-08's page".** The result is true and its two steps are Proposition 4.6.3 and Theorem 4.6.2, both proved on the facing pages; Munkres **proves** the equivalent at printed 267–268 (Theorem 43.5, Theorem 43.6). Settled during the `an2-08` extraction. |
| Lindström §4.8, printed 110–111 | Arzelà–Ascoli (**Theorem 4.8.9**) | **proves**. Corrected during the `an2-03` extraction: the earlier row said "printed 107–109, Proposition 4.8.6". Proposition 4.8.6 is the hard half — a bounded, equicontinuous *sequence* has a convergent subsequence, printed 109 — and the named theorem is 4.8.9 at printed 110–111. §4.8 opens on printed 107 with Definitions 4.8.1 and 4.8.2. |
| Lindström §6.1, printed 174–175 | the derivative as a linear map, and its uniqueness | **proves** |
| Munkres §43, printed 264–272 | completeness; the completion (Theorem 43.7) | **proves** |
| Munkres §45, printed 275 | compact ⟺ complete + totally bounded (Theorem 45.1) | **proves** |
| Munkres §45/§47 exercises | Arzelà's theorem | **sets-as-exercise** — Ascoli (Theorem 47.1) is the proved statement; Arzelà appears in both sections' exercises |

### The `lab` environment

`lab` problem sets contain runnable Python, executed, with real output recorded
as the worked answer (decision of 2026-08-11). Source discipline for a library
becomes: **pinned version + the API surface verified by execution**, cited as
e.g. `gudhi 3.13.0, RipsComplex.create_simplex_tree`. "Every worked example
computes the right answer" becomes "this code was run, in this environment, and
produced this".

The repo's own Python is **3.13.5**, and **giotto-tda publishes no wheel for
3.13** — the only artifact pip offers is a 0.1.4 source tarball. Since giotto-tda
is the first-listed source for six of the nine `lab` units, verification runs in
a dedicated interpreter instead. Registry entries for Python 3.10 and 3.11 exist
on this machine but both point at uninstalled paths; `uv` supplies a managed
3.11 without a system install:

**The commands are pinned, because the recorded outputs are the worked answers.**
An unpinned rebuild resolves whatever is current and can produce different
numbers against the same problem set, with nothing to show that the environment
moved. The version list below is not a footnote — it is the reproduction
instruction, so it appears in the commands themselves:

```bash
uv venv --python 3.11.11 <scratchpad>/labenv
```

```bash
uv pip install --python <scratchpad>/labenv/Scripts/python.exe giotto-tda==0.6.2 kmapper==2.1.0 gudhi==3.13.0 ripser==0.6.15 persim==0.3.8 scikit-learn==1.3.2 numpy==1.26.4
```

`scikit-learn` and `numpy` are pinned although neither was installed by name —
giotto-tda pulled both, and both reach the recorded outputs, so leaving them to
the resolver is exactly the gap this pin closes. If `lab` ends up needing a
committed lockfile rather than a documented one, that is a repo-structure change
and belongs to the `lab` PR, not to this content-only branch; it is listed under
Open decisions.

Built and verified 2026-08-11 — **Python 3.11.11 · giotto-tda 0.6.2 ·
kmapper 2.1.0 · gudhi 3.13.0 · ripser 0.6.15 · persim 0.3.8 ·
scikit-learn 1.3.2 · numpy 1.26.4**, all importing. Two notes for whoever rebuilds
it: the Kepler-Mapper distribution is **`kmapper`**, not `kepler-mapper` (which
does not exist on PyPI, whatever the docs call the project); and the repo-level
3.13 environment already carries gudhi 3.11.0, ripser 0.6.14, persim 0.3.8 and
scikit-learn 1.8.0, which are *different versions* from the venv's — so **every
recorded output must name the environment it came from.** The venv lives in the
session scratchpad and dies with the session; the pinned versions above are the
durable record.

---

## Tracker

Legend: `—` not started · `P` problem set done · `L` lesson written ·
`G` gates green · `✓` complete.

**Update the row in the same commit as the unit.**

### top — Topology (Munkres; Hatcher for top-13)

| Unit | Title | Source | State |
|---|---|---|---|
| top-01 | Topological spaces and open sets | Munkres §12 | ✓ |
| top-02 | Basis, order, and subspace topologies | Munkres §13, §14, §16 | ✓ |
| top-03 | Closed sets, closure, interior, and limit points | Munkres §17 | ✓ |
| top-04 | Continuous functions and homeomorphisms | Munkres §18 | ✓ |
| top-05 | The product topology | Munkres §15, §19 | ✓ |
| top-06 | The metric topology | Munkres §20, §21 | ✓ |
| top-07 | Connectedness and components | Munkres §23, §24, §25 | ✓ |
| top-08 | Compactness | Munkres §26, §27 | ✓ |
| top-09 | Limit-point and local compactness | Munkres §28, §29 | ✓ |
| top-10 | The countability axioms | Munkres §30 | ✓ |
| top-11 | Separation axioms and normal spaces | Munkres §31, §32 | ✓ |
| top-12 | The quotient topology | Munkres §22 | ✓ |
| top-13 | Quotient constructions: cones, suspension, CW complexes | Munkres §22 · Hatcher ch. 0 | ✓ |

> **Gate 1 is vacuous on `top-01`, and correctly so.** `§12 numbers nothing` —
> the definitions are unnumbered, the examples are `EXAMPLE 1`–`4`, and there is
> no theorem, lemma or corollary in the section — so
> `check_lesson_coverage.py` reports `UNCHECKED checked 0 refs` and exits 0.
> That is the source's shape, not an authoring shortcut, and inventing numbers
> to feed the gate would be worse than the vacuity. The disposition is recorded
> in the lesson footer. Expect the same for any `top` unit drawing on a section
> with no numbered results; `--min-refs` becomes usable from `top-02` on, where
> Munkres starts numbering (Lemma 13.1, Lemma 13.2, …).

**`top` is complete — 13 of 13**, each passing all eight gates plus the canvas
read-back. Evidence per unit is in its commit message.

**Source-boundary findings from `top`, for the syllabus pass.**

- **`top-13` and `at1-02` read the same Hatcher pages.** `at1-02`'s sources are
  ch. 0 pp. 5–14; `top-13`'s are pp. 5–10, and `at1-02`'s own problem set says its
  operations "are top-13's quotient constructions". The split adopted:
  `top-13` owns the point-set half (each construction as a quotient), `at1-02`
  owns the homotopy-theoretic half (Proposition 0.17, collapsing a contractible
  subcomplex, the two criteria). Nothing in `top-13` uses homotopy equivalence.
  Worth confirming deliberately rather than leaving to precedent.
- **`at1-01` attributes the universal property to `top-13`; it is Munkres
  Theorem 22.2, in §22 — which is `top-12`'s resource *and* `top-13`'s.** The
  citation is satisfied, but two units share one section. Decide whether §22
  should belong to `top-12` alone.
- **`top-07`'s mission strip is true only under a hypothesis its own parenthesis
  supplies.** "Path components ARE ... the connected pieces" needs local path
  connectedness (Theorem 25.5); the topologist's sine curve has one component and
  two path components. The clause "for TDA's finite complexes" is where the
  hypothesis enters, and whether finite complexes satisfy it belongs to `at1-07`
  and `lab-02`.
- **`top-10`'s hook claims second countability "powers half the metrization
  results".** This unit read the statement of no metrization theorem. Either
  soften the hook or point it at §34.
- **Five mission strips assert properties of point clouds that finiteness settles
  in advance.** `top-06` (discrete), `top-08` (compact), `top-09` (locally
  compact), `top-10` (all four countability axioms), `top-11` (Hausdorff). Each
  strip is true and none distinguishes one point cloud from another of the same
  size. The strips would be informative if re-pointed at the sampled space.

**Review round on PR #17 — what the eight gates could not see.** CodeRabbit
raised ten comments, nine distinct issues, and every one was valid. Eight were
citation, wording or count defects. The ninth was not:

- **`top-03` instructed the reader to prove a false theorem.** "Prove that A′ is
  closed, for every subset A of every space" is false without T₁; the Strategy,
  Partial and Worked start all committed to the proof. Counterexample: X = {a,b}
  indiscrete, A = {a}, A′ = {b}, not closed. Enumerating **every** topology on
  two and on three points afterwards confirmed two points suffice and 33 witnesses
  exist on three. The unit had already recorded that the claim was absent from
  §17 — it went beyond the source knowingly and then got the mathematics wrong on
  its own authority, which is why the source read-back did not catch it either.
  Now reframed as a decide-and-justify task, with the counterexample and the T₁
  theorem in the answer key and the failing step named in the lesson.
  Logged as `2026-08-12-gates-cannot-see-a-false-theorem`.
- **A folio error survived a folio-discipline pass, and the review's suggested
  fix was also wrong.** Theorem 17.3's statement was cited at printed 94 in the
  problem set and 94–95 in the lesson; CodeRabbit proposed 94–95. Reading the
  folios off PDF 111 and 112 shows Munkres announces the theorem with "we leave
  the proof to you" as the **last sentence of printed 94** and states it at the
  **top of printed 95**. The statement is on 95 alone. Both files now say so.
- **Practice adopted for the rest of S2:** a lesson that asserts a result its
  source does not contain must declare it in the footer under *Stated here but
  not in the source*, and any claim small enough to settle by exhaustion is
  settled by exhaustion before it ships.

### an2 — Analysis II (Lindström; Abbott)

Cited sources re-pointed — see Open decisions. Every row's "Source" column is
what the unit will actually be authored from, not what `syllabus.yaml` currently
says.

| Unit | Title | Source | State |
|---|---|---|---|
| an2-01 | Metric-space topology, deepened | Lindström 3.1–3.3 · Abbott 8.2 | ✓ |
| an2-02 | Completeness and completion | Lindström 3.4, 3.7 | ✓ |
| an2-03 | Compactness: sequential, total boundedness, Arzelà–Ascoli | Lindström 3.5, 3.6, 4.8 | ✓ |
| an2-04 | The contraction mapping theorem and applications | Lindström 3.4, 4.7, 4.9 | ✓ |
| an2-05 | Uniform convergence of derivatives and integrals | Abbott 6.2–6.3 · Lindström 4.2–4.3 | ✓ |
| an2-06 | Series of functions; the Weierstrass M-test and approximation | Abbott 6.4–6.6 · Lindström 4.3, 4.10–4.11 | ✓ |
| an2-07 | Normed and Banach spaces | Lindström 5.1, 5.2 (p. 141 only), 5.4 | ✓ |
| an2-08 | Function spaces: C[a,b] | Lindström 4.5, 4.6 · Munkres §46, §43 (pp. 267–268) | ✓ |
| an2-09 | Differentiation in Rⁿ | Lindström 6.1, 6.2, 6.6, 6.7 (pp. 206–208) | ✓ |

**Source-boundary findings from `an2-01`, for the syllabus pass.**

- **`an2-01`'s mission strip is false as stated, and the corpus already knows
  it.** The strip is "The space of persistence diagrams is a metric space;
  fluency here is fluency there." `tda1-06` reads Oudot at printed 51 — the
  bottleneck distance "is an extended pseudometric … It is not a true distance
  though" — and exhibits ℚ² ∖ Δ and (ℚ+√2)² ∖ Δ as distinct diagrams at distance
  zero. Two failures, not one: positivity's "only if" half, and the codomain,
  since "extended" means [0, ∞] where Definition 3.1.1 requires [0, ∞). The
  claim is true on **locally finite multisets in ℝ² ∖ Δ**, the class Oudot's
  Theorem 1.13 delivers for 𝔮-tame modules, and `tda1-06` establishes it there.
  Quoted verbatim per gate 8 and bounded in a `.warn` block; the lesson turns the
  failure into the exercise, since a checklist is worth having precisely because
  the headline application fails it. **For the syllabus pass: re-point the strip
  at the restricted class, or drop "is a metric space" for "carries a distance".**
- **`tda1-05`'s contract is honoured, and its prediction is wrong in an
  instructive way.** `tda1-05` Problem 5(c) hands the reader "an2-01's axioms" as
  a checklist and asks which is "least obviously true" for a distance defined by
  optimising over matchings — expecting the triangle inequality. It holds:
  composing an ε-matching with an ε′-matching gives an (ε+ε′)-matching, which is
  the same one-witness-bounds-the-optimum move as the sup metric's triangle
  inequality in Lindström Example 5. The axiom that actually fails is positivity.
  `an2-01` sets up the checklist so that this comes out, rather than confirming
  the prediction. **No change needed to `tda1-05`; its framing is open enough.**
- **`an2-01` cites no `top` unit as a prerequisite and must not assume one.**
  `an2`'s chain runs from `an-14` and never touches `top`, so the two S2 modules
  are parallel. Lindström disclaims topological spaces outright — "We shall not
  look at topological spaces in this book" (printed 57) — while supplying every
  ingredient for the metric topology in Propositions 3.3.12 and 3.3.13. The
  lesson therefore derives 𝒯_d itself and flags it under *Stated here but not in
  the source*, rather than citing `top-01`.

**Source-boundary findings from `an2-02`, for the syllabus pass.**

- **`an2-02`'s mission strip attaches completeness to the wrong theorem.** The
  strip is "Diagram space is complete; completeness is what lets limits of
  diagrams exist to be proven stable." The first clause is true under a
  restriction it does not state: `tda2-09` reads Dey and Wang at printed 406,
  where Definition 13.9 defines 𝔻_q^p by a finiteness condition and 𝔻_q^∞ is
  Polish — complete *and separable*. The second clause is wrong. Stability, as
  `tda1-07` has it from Oudot's Corollary 3.6, is
  `d_b(dgm f, dgm g) ≤ ‖f − g‖_∞` for two 𝔮-tame functions on the same space:
  no sequence, no limit, and completeness nowhere in its hypotheses or proof.
  What completeness underwrites in the corpus is `tda2-09`'s Theorem 13.10, the
  non-emptiness of the Fréchet expectation — statistics, not stability. **For
  the syllabus pass: re-point the second clause at Fréchet means, or drop it.**
- **`an2-01`'s pseudometric finding is a precondition for `an2-02`'s strip, not
  a footnote to it.** Definition 3.4.3 says "all Cauchy sequences converge", and
  in a pseudometric space limits are not unique — Proposition 3.2.3 derives
  uniqueness from exactly the half of positivity that the bottleneck distance
  fails. So "complete" has no single meaning on diagrams until the class is
  restricted. The two strips should be re-pointed together or not at all.
- **`an2-02` hands `an2-08` a promise Lindström makes and defers.** Example 2 to
  §3.4 (printed 60) asserts that only `d₁` of an2-01's three metrics on
  `C[a,b]` is complete and says "we shall prove this in the next chapter". The
  proof is **Theorem 4.6.2, printed 101** — inside `an2-08`'s §4.6 — and it
  cites Proposition 3.4.4 from this unit. `an2-08` must pick it up; folio
  re-verified (PDF 114 → 101).
- **Lindström sets one lemma as an exercise and then proves it.** Exercise 8 to
  §3.4 (printed 63) is Lemma 3.7.11 (printed 75) word for word. Not a defect —
  but it is the second modality finding in `an2` where one result carries two
  modalities in one book, so the footer records both.

**Source-boundary findings from `an2-03`, for the syllabus pass.**

- **`an2-03`'s strip names the right mechanism and the wrong two spaces.** The
  strip is "Compactness of diagram and function spaces is the engine behind
  existence proofs in stability theory." The mechanism is right and is the
  best-supported claim of any strip in `an2`: `tda1-06` reads Oudot at printed 51
  where a compactness argument makes the bottleneck infimum *attained* on locally
  finite multisets in ℝ² ∖ Δ, and "an infimum over a compact set is attained" is
  Theorem 3.5.10, this unit's centrepiece. But **no function space here is
  compact** — Lindström's Exercise 7 to §4.8 (printed 112) sets exactly that
  `C([0,1], ℝ)` is not even *locally* compact, and Theorem 4.8.9 characterises
  compact *subsets*. And **the corpus settles nothing about compactness of
  diagram space**: `tda2-09` reports 𝔻_q^∞ Polish, and Proposition 3.5.7 plus
  Proposition 4.8.3 give compact ⟹ complete + separable with the converse false
  (ℕ discrete). **For the syllabus pass: re-point at attained infima and compact
  subsets, and drop "diagram and function spaces".**
- **Both corpus appeals to compactness point outside their own pages.**
  `tda1-06` records Oudot attributing the attainment argument to a reference and
  not reproducing it; `tda1-07` records "the compactness argument needed when
  dgm(𝕌ₓ) is infinite is referred to [72, §4.8] and not carried out". So the
  strip's "in stability theory" names the one branch where the corpus has never
  seen the argument. The one visible existence use is `tda2-09`'s Theorem 13.10,
  whose hypothesis is *compact support* — statistics again, as with `an2-02`.
- **Modality-table correction, made above.** Arzelà–Ascoli is **Theorem 4.8.9 at
  printed 110–111**, not "Proposition 4.8.6, printed 107–109". 4.8.6 is the hard
  half (bounded + equicontinuous sequence has a convergent subsequence, printed
  109). Found by folio read during extraction; the old row would have produced a
  wrong citation in any unit that trusted it.
- **An extraction defect, not a source defect.** The per-page markdown of printed
  70 renders **Corollary 3.6.5 with only its clause (ii)** — clause (i) is
  missing from the extraction. Theorem 3.6.4 has already identified compactness
  with the open covering property, so (i) is one of those two and nothing turns
  on which; the lesson and set say so rather than inventing a wording. Worth
  knowing that the per-page markdown can silently drop an enumerated clause.
- **The coverage gate's wrapped-citation defect fired a fourth time, and this
  time it corrupted the denominator.** `an2-03`'s problem set cited
  Theorem 3.5.13 both wrapped and unwrapped, and the gate counted them as two
  refs — 28 reported against 27 real. Reflowed, for the fourth time, because gate
  edits are out of scope here. Recurrence and the denominator finding added to
  `2026-08-10-coverage-gate-breaks-on-wrapped-citations`.

**Source-boundary findings from `an2-04`, for the syllabus pass.**

- **The first `an2` strip that needs no rescoping.** "Fixed-point arguments recur
  wherever an algorithm's convergence must be proven, not merely hoped" is true
  and the unit earns it outright: Theorem 3.4.5 makes an iteration a proof,
  (3.4.1) makes it a rate, and completeness is what both cost. Recorded here
  because three consecutive rescopings could otherwise read as a house style
  rather than a finding — this one is a control on the other three. **No syllabus
  action.**
- **A source discrepancy in Exercise 2 to §4.7, and it cannot be adjudicated from
  the markdown.** The per-page extract of printed 106 gives the equation as
  `y′ = (3/2)y^(1/4)`, which is inconsistent with the solution family
  `(t − a)^(3/2)` printed immediately below it — that family solves
  `y′ = (3/2)y^(1/3)`. Whether the slip is the book's or the extraction's needs
  the PDF page image, which the per-page markdown does not carry. Both files use
  1/3, state the discrepancy, and tell the reader to check the arithmetic. This
  is the second extraction anomaly in `an2` after Corollary 3.6.5's dropped
  clause; **for the syllabus pass, worth deciding whether the corpus needs a
  page-image fallback for adjudicating cases like this.**
- **`an2-04` supplies a sharpness result Lindström does not.** He states (3.4.1)
  and calls it "very precise information" without exhibiting a case of equality.
  An affine contraction `x ↦ sx + c` attains it at every n and every starting
  point, because `x_n − a = s^n(x₀ − a)` and `x₁ − x₀ = (1 − s)(a − x₀)`. So no
  constant below `1/(1 − s)` can replace it. Flagged in the footer; the figure is
  built on it.
- **Forward dependency for `an2-09`.** Lindström says at printed 62 that
  Theorem 3.4.5 will be used in Chapter 6 to prove the Inverse and Implicit
  Function Theorems. `an2-09`'s sources are §6.1, §6.2 and §6.6; whichever of
  those carries those theorems must pick the reference up.

**Source-boundary findings from `an2-05`, for the syllabus pass.**

- **The strip is true and unusually literal, and the finding is what it does
  *not* license.** "Sup-norm (uniform) control is the exact language the
  persistence stability theorem is written in" — and it is: `tda1-07` states
  stability as Oudot's Corollary 3.6, `d_b(dgm f, dgm g) ≤ ‖f − g‖_∞`, whose
  hypothesis is smallness in an2-01's sup metric `d₁`, which by Proposition 4.2.3
  *is* uniform convergence. What this unit adds is the transfer table: sup-norm
  closeness carries continuity (Theorem 6.2.6), uniform continuity, boundedness
  and the value of an integral (Proposition 4.3.1), and carries **neither
  differentiability nor the value of a derivative**. So a sup-norm hypothesis on
  a filter function licenses nothing about gradients of filtrations. **No
  syllabus action on the strip; the constraint belongs in any downstream unit
  that reasons from a sup-norm bound to a gradient.**
- **The `an2-06` decision recorded under Open decisions rests on a modality error
  and must be revisited.** That entry says the M-test is taken from Abbott
  "while **Lindström sets it as Exercise 11 of §4.2** (printed 85)". Lindström
  *also* states and **proves** it as **Proposition 4.3.4 at printed 89**. The
  modality table above is corrected. The choice of source for `an2-06` is now
  open on its merits rather than settled by availability, and `an2-05` already
  cites Proposition 4.3.4 because Corollary 4.3.3 needs a way to certify uniform
  convergence of a series.
- **Second instance of one result carrying two modalities in Lindström.** The
  M-test at printed 85 (set) and printed 89 (proved) joins Lemma 3.7.11, set as
  Exercise 8 to §3.4 at printed 63 and proved at printed 75. Two occurrences in
  five units is a pattern, not a coincidence: **a citation's verb is a property
  of the page cited, not of the result, and must not be inferred from the
  section.** Recorded in `an2-05`'s footer and in a self-check.
- **Abbott writes a proof in order to break it, and the modality register has no
  word for that.** At printed 156–157 he attempts to prove that a pointwise limit
  of continuous functions is continuous, controls two of three terms, and stops
  at the third. It is neither `proves` nor `states` nor `sets-as-exercise`; the
  lesson footer records it in words. **For the syllabus pass: consider whether
  the modality vocabulary needs a fifth value for deliberately failed
  arguments** — `top-03`'s defect was an argument that failed *without* the book
  saying so, which is the same object with the disclosure removed.

**Source-boundary findings from `an2-06`, for the syllabus pass.**

- **The M-test source decision, remade on merits.** The earlier entry chose
  Abbott because "Lindström only sets it as Exercise 11 of §4.2". That reason was
  void (he also proves it as Proposition 4.3.4). Remade from the pages: Abbott's
  **Corollary 6.4.5**, printed 168, is a corollary of his **Theorem 6.4.4**,
  which he states with **no proof and no exercise attached**, and 6.4.4 is
  an2-05's Theorem 6.2.5 in series clothing — itself set as Exercise 6.2.6. So
  Abbott's route to the M-test passes through two results neither of which is
  established where it stands. Lindström's **Proposition 4.3.4**, printed 89,
  states it for functions on an *arbitrary* set and proves it in four lines from
  the completeness of ℝ. **Decision: the proof is Lindström's, the placement and
  the applications are Abbott's, and the lesson says which at each use.** The
  statements also differ in hypothesis — Abbott needs `M_n > 0`, Lindström only
  `M_n ≥ 0` — which is recorded because it is the kind of difference a citation
  hides. Task #8 is closed by this entry.
- **§4.3 is added to the unit's Lindström range.** Proposition 4.3.4 is in §4.3,
  which the tracker row listed only against `an2-05`. Row updated.
- **Abbott's §6.5 establishes two of its seven results.** Proved in place:
  Theorem 6.5.1 and Theorem 6.5.4 (Abel). Delegated: Theorem 6.5.2 (Ex. 6.5.5),
  Lemma 6.5.3 (Exs. 2.7.12 and 2.7.14(b), two chapters back), Theorem 6.5.5
  (Ex. 6.5.6), Theorem 6.5.6 (Ex. 6.5.7); Theorem 6.5.7 partly. This is not a
  complaint about the book — it is a constraint on citation, and the lesson
  carries the audit as a table. **No syllabus action; recorded as evidence for
  the standing rule that a citation's verb is a property of the page.**
- **Neither book proves anything about Cauchy's counterexample.** `e^(−1/x²)` is
  presented as running text by Abbott at printed 180 with every step set as
  Exercises 6.6.9–6.6.12, and set outright by Lindström as Exercise 3 to §4.10 at
  printed 122. The lesson's guided proof is therefore its own work and is written
  to reason only from properties both books assert. Attribution, from Abbott's
  epilogue at printed 181: Cauchy, 1821.
- **The strip is true and names the wrong theorem.** "Uniform approximation
  justifies discretising a continuous signal before you compute its topology" is
  sound as an implication — a bound on ‖f − g‖∞ feeds tda1-07's report of Oudot's
  Corollary 3.6. But **Theorem 4.10.1 is not what supplies the g** when g is a
  sampled signal: Weierstrass produces *polynomials*, by an existence argument
  with no rate. What bounds ‖f − f_grid‖∞ is uniform continuity on a compact
  domain (Proposition 4.1.2) plus the grid spacing. **For the syllabus pass: no
  change to the strip; the constraint is that any downstream unit arguing from it
  should cite uniform continuity and stability, not Weierstrass.**
- **`an2-06`'s hook is redeemed, and only on a compact interval.** "Approximate
  ANY continuous function by polynomials" is Theorem 4.10.1, printed 116, proved
  twice. It is *false* off a closed bounded interval: Lindström's Exercises 1 and
  2 to §4.10 (printed 122) rule out uniform polynomial approximation of `1/x` on
  `(0,1)` and of `e^x` on ℝ. The earlier Open-decisions entry proposed extending
  the unit's Abbott range to §8.3 for the approximation theorem; that is now
  **unnecessary** — Lindström §4.10 carries it with proof, and Abbott's printed
  242 version is set as Exercise 8.3.10. **For the syllabus pass: `an2-06`'s
  resource line should read Lindström 4.3, 4.10–4.11 alongside Abbott 6.4–6.6;
  the Abbott range does not need to reach 8.3.**
- **No S1 unit establishes the Riemann integral or the Fundamental Theorem of
  Calculus, and two `an2` units depend on both.** The `an` module runs an-01 to
  an-14 with no integration unit: an-12 is the derivative and the MVT, an-13 is
  uniform convergence. `an2-05`'s guided proof uses the FTC twice and `an2-06`
  uses term-by-term integration throughout. `an2-05`'s footer cited "an-13's" for
  the FTC, which was wrong on both counts and **has been corrected in this commit**
  to say plainly that no S1 unit carries it. **For the syllabus pass: S1 needs an
  integration unit, or the two `an2` units need their dependency stated as an
  external assumption.**

**Source-boundary findings from `an2-07`, for the syllabus pass.**

- **The unit's title names an object its cited sections do not define.**
  "A complete normed space is known as a *Banach space*" is one sentence in
  **§5.2, printed 141**, immediately before **Proposition 5.2.3**; the resource
  line said §5.1 and §5.4. The unit takes those two items from printed 141 and
  nothing else from §5.2, and says so at every use. **For the syllabus pass:
  `an2-07`'s resource line should read Lindström 5.1, 5.2, 5.4.**
- **The strip's second clause is true and one structure short, and the downstream
  unit already says the stronger thing.** "Feature spaces for vectorised diagrams
  are normed spaces; kernels live in complete (Banach) ones." Clause one is
  exactly right. Clause two: `tda2-09` defines the persistence scale space kernel
  as `k_σ(D,E) = ⟨Φ_σ(D), Φ_σ(E)⟩` in `𝓛²(Ω)` — an **inner product** — and its own
  strip says diagram space "is a metric space but not a Hilbert space, which is
  precisely why lab-06's landscapes, images and kernels exist". An inner product
  is **Definition 5.3.1, printed 142**, in §5.3, one section outside the cited
  range; the lesson states it in the warn block and does not develop it.
  Completeness is necessary and it is the inner product that makes a kernel a
  kernel. **For the syllabus pass: either extend `an2-07` to §5.3, or change the
  strip's second clause to say Hilbert and name the unit that supplies it.** This
  is the same *shape* as `an2-03`'s finding — mechanism right, named object
  wrong — but milder: the strip understates rather than overstates.
- **Chapter 5 opens on printed 133, not 134 — the second instance of the
  chapter-opener pattern.** PDF 146 carries the chapter title "Normed Spaces and
  Linear Operators", the §5.1 heading **and** Definition 5.1.1(i), with folio 133
  at the foot. The section-start table listed §5.1 at 133 and the chapter at 134,
  which cannot both be right; the same self-contradiction was found and fixed for
  Chapter 3 during the `an2-01` extraction. **Two occurrences were taken to make
  it a rule: in this book a chapter opener is not a blank page, and a chapter
  starts on the same folio as its first section.** Section-start table corrected
  below. *(Withdrawn at `an2-09`; see that unit's findings. Chapter 4 was later
  found to be a third instance — PDF 92 carries the title, the §4.1 heading and
  folio 79 — which did not save the rule, because Chapter 6 refutes it.)*
- **Two carried folios corrected.** `Lemma 3.5.6` and `Theorem 3.5.10` are both
  on **printed 65** (PDF 78), not 64 and 66 as two earlier drafts had them. Both
  are `an2-03`'s pages; `an2-06`'s footer was corrected in the same sweep.
- **An extraction anomaly, stated rather than smoothed.** The Inverse Triangle
  Inequality for norms renders **without** its outer absolute-value bars at
  printed 136 (formula (5.1.2)), in the proof of Proposition 5.1.4(i), and in
  Exercise 5 to §5.1 — and **with** them inside the proof of Theorem 5.4.8 at
  printed 154 and in the metric version, Proposition 3.1.4, at printed 47. Both
  readings are true and every proof in the unit works with either, so nothing
  depends on it. Not adjudicable from the markdown; recorded, not resolved. Third
  such anomaly in `an2`, after the dropped clause of Corollary 3.6.5 at printed 70
  and the exponent in Exercise 2 to §4.7 at printed 106.
- **`an2-07` supplies the mechanism `an2-05` recorded without one, and Lindström
  says so himself.** an2-05's transfer table had "the value of f′ does not
  transfer", witnessed by `sin(nx)/n → 0` with `cos(nx)` divergent. Lindström's
  **Example 4 to §5.4, printed 152**, is the same two functions: `u_n = sin nx`
  has `‖u_n‖ = 1` and `‖D(u_n)‖ = n`, so differentiation is an **unbounded**
  operator — and by **Theorem 5.4.5** unbounded and discontinuous are one word for
  a linear map. He adds that this "is the source of a lot of trouble, e.g., the
  rather unsatisfactory conditions we had to enforce ... in Proposition 4.3.5",
  naming an2-05's proposition. **No syllabus action; recorded because it is the
  strongest forward reference any `an2` unit has turned out to have, and it was
  invisible from the resource lines.**
- **The Analysis III finding is confirmed by authoring.** The earlier entry said
  `an2-07` and `an2-08` should not cite "Oxford M2 Analysis III notes", since that
  volume is integration and contains neither normed spaces nor `C[a,b]`. `an2-07`
  is authored entirely from Lindström Chapter 5 and needed nothing from Oxford.
  **For the syllabus pass: replace `an2-07`'s resource line outright.**

**Source-boundary findings from `an2-08`, for the syllabus pass.**

- **The module's most-cited theorem is stated, not proved — and the audit had to
  reach `an2-08` to find out.** **Theorem 4.6.4** at printed 102 is cited as the
  supplier of completeness by **six** earlier lessons, each recording it as
  "an2-08's page". Lindström states it: "The following theorem sums up the results
  above for X compact", with no proof and no exercise attached. The theorem is
  **true** — its two steps are Proposition 4.6.3 (compact ⟹ continuous is bounded,
  proved) and Theorem 4.6.2 (C_b complete, proved) — and Munkres **proves** the
  equivalent statement outright at printed 267–268 as Theorem 43.5 and Theorem 43.6
  plus a running-text remark for the compact case. So no citing lesson asserted
  anything false; what none of them may write is "Lindström proves Theorem 4.6.4".
  The lesson's guided proof supplies the two steps. **Row added to the modality
  register above. No syllabus action, but this is the clearest evidence yet for
  the standing rule: a citation's verb has to be read off the page, and a result
  that is two lines from a proof is still not proved.**
- **Munkres §43 is needed and is outside the cited range.** Theorem 43.5 and
  Theorem 43.6 at printed 267, with the compact case at printed 268, are what
  actually prove Theorem 4.6.4. The unit takes those two pages and nothing else
  from §43. **For the syllabus pass: `an2-08`'s resource line should read Munkres
  §46 and §43.** This is the third unit in the module whose title or spine needs a
  section its resource line omits, after `an2-06` (§4.3 for the M-test) and
  `an2-07` (§5.2 for the definition of a Banach space).
- **The strip is true with a hypothesis, and the hypothesis is compactness.**
  "Filtration functions live in C(X); treating a whole space of functions as one
  object is the key move." Both clauses hold — Lindström calls the move "a bold
  step" himself, and `an2-04` already cashed it. What the unit adds is that
  **"C(X)" names a set and three topologies**: Munkres's Theorem 46.7 orders them
  uniform ⊃ compact convergence ⊃ pointwise, and the first two coincide **exactly
  when X is compact**. Worse, without compactness ρ may be infinite and is not a
  metric at all — Lindström's own warning at printed 102, with Exercise 1 to §4.6
  as the witness. A stability bound written with `‖f − g‖_∞` is therefore a
  statement in the finest of the three, presupposing a compact domain.
  **No syllabus action; the constraint belongs in any downstream unit that writes
  C(X) without saying what X is.**
- **Two typographical slips in Lindström, both harmless, both recorded.** The proof
  of Proposition 4.5.1 at printed 99 says "if f, g, h are three functions in
  C(X, Y)" where the proposition is about B(X, Y). The displayed inequality inside
  Proposition 4.6.3's proof at printed 102 renders `|d_Y(f(x), a) − d_Y(f(y), a)|`,
  which is ill-formed — a ∈ X, and the metric is on Y — where h's own definition
  two lines above gives `|d_Y(f(x), f(a)) − d_Y(f(y), f(a))|`. Neither affects an
  argument; both are stated in the lesson footer rather than silently corrected.
- **A notation collision inside one book.** Exercise 6 to §4.5 at printed 101
  defines `c_0` as the **bounded** real sequences under the sup metric; Example 1
  to §5.2 at printed 140 defines `c_0` as the sequences tending to 0. Both of
  Lindström's claims are true of the object he defines in each place, and the
  spaces are different — standard usage calls the first `ℓ^∞`. Recorded because a
  unit citing "Lindström's c_0" without a page would be ambiguous, and `an2-07`
  cites the second while `an2-08` cites the first.
- **Two more carried folios corrected.** `Proposition 3.3.7` is on **printed 55**
  (PDF 68) and `Proposition 3.4.4` on **printed 60** (PDF 73); an earlier draft of
  this unit's footer had 56 and 61. `an2-02`'s lesson already had 3.4.4 at 60.
- **`an2-08` closes `top-04`'s promise to `at1-01` with a theorem.** top-04 told
  at1-01 that a homotopy is one **jointly** continuous map on X × I, not a family
  of continuous maps. Munkres's **Theorem 46.11** at printed 287 is the exponential
  law: joint continuity of f : X × Z → Y always gives continuity of the induced
  F : Z → 𝒞(X, Y), and the converse holds **only if X is locally compact
  Hausdorff**. So joint continuity is strictly the stronger description and
  top-04's insistence was exactly right. **No syllabus action; recorded because it
  is a cross-semester promise that turned out to have a citable proof.**
- **The Analysis III finding is confirmed a second time.** `an2-08` is authored
  entirely from Lindström §4.5–4.6 and Munkres §46 and §43, and needed nothing
  from the Oxford notes. **For the syllabus pass: replace `an2-08`'s resource line
  outright, as with `an2-07`.**

**Source-boundary findings from `an2-09`, for the syllabus pass.**

- **The `Still open` entry on `an2-09`'s source is confirmed by authoring.** The
  syllabus gives "Oxford M2 notes (multivariable analysis)"; that file is vector
  calculus with **zero occurrences of "linear map"**, and the hook it carries
  promises exactly that. The unit is authored from Lindström Chapter 6 and needed
  nothing from Oxford. **For the syllabus pass: re-point `an2-09` to Lindström
  6.1, 6.2, 6.6, 6.7.**
- **The chapter-opener rule recorded at `an2-07` is false, and is withdrawn
  here.** Chapter 6 opens on printed **173**, with a full page of introduction and
  no section heading; §6.1 begins on printed 174. Chapters 3, 4 and 5 do put the
  first section's heading on the opener page — 43, 79 and 133 — so the pattern is
  real for three chapters out of four and still false as a rule. The section-start
  table has been corrected. **This is the procedural finding of the unit: a
  pattern observed and generalised is a hypothesis however many instances it has,
  and the plan's own standing instruction — read the folio off the page you cite —
  is what caught it one unit later.**
- **The forward promise from printed 62 lands outside the range.** `an2-02`
  recorded Lindström saying that Theorem 3.4.5 would be used in Chapter 6 for the
  Inverse and Implicit Function Theorems, and `an2-04` passed it on as "Chapter 6
  — an2-09's". The destination is **§6.7**: Theorem 6.7.1 at printed 206, and the
  collection point is **Lemma 6.7.2** at printed 207, where Lindström writes "We
  shall now use Banach's Fixed Point Theorem 3.4.5 to prove this" and applies it
  to `K(x) = y − H(x)`. §6.7 is one section past the cited range and is taken for
  that purpose only. **Fourth unit in the module whose spine needs a section its
  resource line omits**, after `an2-06` (§4.3), `an2-07` (§5.2) and `an2-08`
  (Munkres §43). **For the syllabus pass: this is a pattern in the resource lines,
  not four coincidences — every `an2` resource line should be re-derived from the
  unit's actual content rather than patched item by item.**
- **A deferral that is honoured, in contrast with `an2-08`'s.** Proposition 6.2.1
  at printed 184 is **stated with the proof deferred**: "As we shall prove a more
  general version of this result in Section 6.6, I skip the proof here." §6.6 is
  in range and Theorem 6.6.3 at printed 203 is the promised version, proved. The
  lesson's guided proof also **verifies the deferral is not circular** — none of
  Propositions 6.6.2, 6.6.4, 6.6.5 or Theorem 6.6.3 cites 6.2.1, directly or
  transitively. **This is a distinct modality from `an2-08`'s Theorem 4.6.4, which
  was stated with no forward pointer at all, and the register should distinguish
  them: `states-deferred` is a checkable claim about the book's dependency graph;
  `states` is not.**
- **One defect of my own, caught in the source read-back.** A first draft of the
  lesson and set said Proposition 6.6.2 is proved "by the chain rule applied to
  the affine injection". It is not: Lindström's proof identifies the partial map's
  error term `σ_i(r_i)` with `F`'s own error term at `r̂_i` and appeals to
  Definition 6.1.1 alone. Corrected in both files before commit, and the
  circularity walk in the guided proof was corrected with it. The chain-rule route
  does work and is longer.
- **The strip is true and what it licenses is a constraint.** "Morse theory and
  gradient-based filtrations need the multivariable derivative treated honestly."
  Honest treatment turns out to say: the derivative is the linear map
  (Definition 6.1.1); `∇f(a)` is the vector *representing* that functional through
  an inner product — an2-07's Definition 5.3.1, printed 142, developed in no S2
  unit, and unavailable on `C([0,1],ℝ)` with the supremum norm; and an2-05's
  transfer table still binds, so a bound on `‖f − g‖_∞` licenses nothing about
  `∇f − ∇g`. **No syllabus action; the constraint belongs in any downstream unit
  that builds a filtration from a gradient and then perturbs the function.** This
  is the third `an2` strip whose audit ends in a constraint rather than a
  correction, after `an2-05` and `an2-08`.

**Findings from the PR #18 review pass (CodeRabbit and Codex), for the syllabus
pass and for the extraction tooling.**

- **An extraction dropped a hypothesis, silently, and the source read-back agreed
  with it.** The repository markdown of Lindström printed 70 renders
  Corollary 3.6.5 as "Assume that `K` is a subset of a metric space `X`. Then the
  following are equivalent:" followed immediately by "(ii) …". The line "(i) `K`
  is compact." is absent from the extract and present in the PDF (page 83, read
  with pymupdf). `an2-03` therefore restated the corollary without its hypothesis,
  in both files, and the restatement is *false* without it — `K = ℝ` with
  `Fₙ = [n, ∞)` has the finite intersection property and empty total
  intersection. Both files corrected, and the problem set now carries the note.
  **This is a defect in the ingest layer, not in the authoring procedure**, and it
  defeats every downstream check that reads the extract rather than the page. The
  one surviving signal was an enumerated list beginning at `(ii)`. **Recommended:
  a structural check at extraction time — flag any enumerated list whose labels do
  not start at the first marker of their series, and any "the following are
  equivalent:" not followed by at least two labelled clauses.** Logged as
  observation `2026-08-13-extract-drops-hypothesis`.
- **Abbott's Example 6.6.2 misprints the Lagrange remainder for sin.** Printed 179
  gives `E₅(x) = (−cos(c)/6!)x⁶`; the sixth derivative of sin is `−sin`, so the
  numerator is `−sin(c)`. Verified against the PDF, so the slip is the book's and
  not the extraction's — the opposite verdict from the `an2-04` `1/3` versus `1/4`
  case, which could not be resolved. Abbott's numerical conclusion `.089` is
  unaffected, because the bound he needs holds for either function. `an2-06` now
  prints the corrected formula, records what the page says, and says which is
  which. **No syllabus action; this is errata, and belongs wherever the module
  eventually keeps them.**
- **A distractor in `an2-01`'s Self-check 1 was true.** Abbott's Exercise
  8.2.1(a)(iv), `d(x,y) = |x₁x₂ + y₁y₂|`, fails the triangle inequality as well as
  (i): take `x = y = (1,1)` and `z = (1,−1)`. The unit's Problem 1 asks for the
  *first* condition each candidate fails, which is (i) and is what the tally
  counts, so the exercise was sound and the surrounding prose was not. Fixed in
  both files, with the word "first" now doing visible work in the tally.
- **Three claims were stronger than their witnesses.** `an2-03`'s hook said
  "closed and bounded" is a fact about ℝⁿ "and about nothing else", which
  `an2-07` contradicts two units later; `an2-04` said two extractions from the
  Euler sequence for `y′ = (3/2)y^(1/3)` "demonstrably" differ, when in fact
  every Euler polygon from `y(0) = 0` is identically zero; `an2-05` said two
  functions uniformly within `1/n` can have derivatives differing by 2
  everywhere, which is impossible under that bound. In each case the underlying
  point survived and the overstatement did not, and in two of the three the
  corrected version is the better teaching point. **No syllabus action. The
  pattern is worth naming: all three were rhetorical amplifications of a true
  claim, added at the sentence level, and none would have been caught by any of
  the eight gates.**

**Source-boundary findings from `lab-01` and `lab-02`, for the syllabus pass.**

- **Edelsbrunner and Harer number none of their results.** §III.2 carries Helly's
  Theorem (printed 69), the Nerve Theorem (printed 71) and the Vietoris–Rips
  Lemma (printed 74), all named and none numbered. Gate 1 matches a keyword
  followed by a dotted number, so it returns `UNCHECKED checked 0 refs` for any
  `lab` set sourced from this book. This is the vacuity predicted above, arriving
  for a reason that was not predicted — not "library APIs instead of numbered
  results" but "the book does not number". **The honest `--min-refs` for such a
  set is 0, stated with its reason.** Folio offset **−12**, verified by reading
  the running head, which carries the folio as its third element.
- **Gate 1 cannot distinguish use from mention.** `lab-02`'s Problem 5 explains
  gate 1's pattern, and the first draft illustrated it by writing two example
  references out in full. The gate read them as citations by `lab-02` and failed,
  naming two results from another module. No widening fixes this: an example of
  the pattern is an instance of it. The paragraph now describes the pattern
  instead of instantiating it, and the finding is taught in the unit. Logged as
  `2026-08-13-coverage-gate-cannot-tell-use-from-mention`. **Recommended: bound
  the gate's domain explicitly rather than widen it.**
- **`gudhi` 3.13.0 ships no `CechComplex`.** It offers `RipsComplex`,
  `AlphaComplex`, `DelaunayCechComplex`, `DelaunayComplex`, `WitnessComplex` and
  others. The syllabus resource line for `lab-02` names "GUDHI: RipsComplex &
  AlphaComplex tutorials", which is accurate, but a reader who goes looking for
  the Čech complex the theory is about will not find it. `DelaunayCechComplex`
  substitutes the Delaunay-restricted filtration, which has the same persistent
  homology in general position and is **not the same complex** — the
  simplex-counting comparison in `lab-02` Problem 3 cannot be run with it, which
  is why that problem implements the enclosing-ball test by hand. **No syllabus
  action; recorded because the substitution is invisible at the call site.**
- **Three live conventions for one filtration value.** The same 2-simplex on a
  unit equilateral triangle is 1.00000 (gudhi Rips, filtered by diameter),
  0.57735 (gudhi Čech, filtered by enclosing radius) and 0.33333 (the same, under
  the `output_squared_values=True` default). Edelsbrunner's Rips(r) uses
  diam ≤ 2r, so his r is gudhi's `max_edge_length` halved. **This is the lab
  analogue of the folio discipline** and is handled the same way: record the call
  with its version and its defaults, and never quote a filtration value bare.
- **`lab-03`'s resource line is right, and incomplete — checked, not assumed.**
  The flag raised before authoring was that the hook's "one matrix reduction over
  Z/2" might live in §IV.2 rather than the cited ch. VII. Reading both: the
  **persistence algorithm** — `low(j)`, the left-to-right column additions,
  R = ∂·V — is at **VII.1, printed 182**, so ch. VII is correct. §IV.2 "Matrix
  Reduction", printed 101–106, is where reduction is introduced for *ordinary*
  homology, and the unit reads it alongside. **For the syllabus pass: add §IV.2 to
  `lab-03`'s line.** This is the fifth consecutive resource line needing a section
  it omits, after the four in `an2`, and it is now a pattern across two modules.
- **`lab-03` cannot report timings, and gate 9 is why.** The natural way to
  illustrate the "at most cubic" claim is to time the reduction. Gate 9 runs each
  problem set twice and requires the runs to agree, so any elapsed time fails it.
  Cost is reported as **column additions** instead, which is deterministic and is
  the quantity the bound is actually about. Recorded because it is a case of a
  gate constraining content in a way that improved it.

**Source-boundary findings from `lab-06`, for the syllabus pass.**

- **The unit had a book on disk and was not told about it.** `lab-06`'s resource
  line names `giotto-tda: diagram vectorisation` and `scikit-tda persim` and no
  text. **Dey and Wang, ch. 13 §13.1, printed 390–395** defines every object in
  the unit (Definition 13.1 persistence landscape, printed 391; Definition 13.4
  persistence image, printed 395) and states two of its three stability results
  (Theorem 13.1, printed 393; Theorem 13.3, printed 395). It is used as the
  primary source and the divergence is recorded. **For the syllabus pass: add Dey
  and Wang §13.1 to `lab-06`'s line, and §13.3 to `lab-07`'s — ch. 13's remaining
  sections are that unit's subject.** This is a different failure from the
  five-in-a-row section omissions: the line does not name the wrong section, it
  names no book at all while one sits on disk.
- **Dey folio = PDF − 21, verified on eight pages, seven of them consecutive.**
  PDF 411–417 → 390–396, and — checked in the second PR-comment pass, four
  hundred pages away — PDF 96 → 75. The folio is at the **outer edge** of the
  running head: second element on a recto, first on a verso. **Seventh** book
  with a verified offset, after the six already recorded: Lindström −13,
  Abbott −12, Munkres −17, Hatcher −9, Edelsbrunner −12 and Oudot −9. The distant page is the useful one: a
  seven-page run establishes the offset locally, and a check in a different
  chapter is what establishes there is no second pagination scheme.
- **`Theorem 13.3` as printed is false, and the missing hypothesis is in the
  book's prose rather than in its statement.** The theorem asks only that ω be
  nonnegative with bounded gradient and bounded sup norm — nothing about the
  diagonal. Instantiating it with **ω ≡ 1**, which satisfies every printed
  hypothesis with |∇ω| = 0, gives an image difference that plateaus at 0.326 while
  the stated bound falls to 0.001427: an excess factor of **228**, unbounded as
  the lifetime shrinks. The mechanism is elementary and implementation-independent
  — Definition 13.4 places the full weight ω(u) on every point regardless of
  position — so this is a defect in the printed statement, not in persim. Dey and
  Wang's own prose between the definition and the theorem recommends "the
  persistence |b − d|" as the natural ω, which is exactly a diagonal-vanishing
  weight.

  *Corrected in the second PR-comment pass, and the correction is the method.*
  The first two versions of this finding put the excess factor at 323, computed
  against `persim.wasserstein`, which matches in the **Euclidean** ground metric.
  **Definition 3.10, printed 75**, defines d_{W,q} with the **ℓ_q** ground
  metric, so the d_{W,1} of Theorem 13.3 is an ℓ¹ matching cost and every bound
  column was √2 too small. (A reviewer raised the mismatch and proposed ℓ∞, which
  is the persistence literature's usual convention for the *bottleneck* distance
  and is not what this book's Definition 3.10 says — the number had to come from
  the book being cited, not from the field.) It changes no verdict: 228 and 323
  are the same finding. It had to be checked anyway, because the conclusion being
  drawn is **that a printed theorem is false**, and an unexamined factor of 1.41
  on the right-hand side is exactly what would make that conclusion unusable. The
  unit now runs the convention check as its own table before the bound table. **This is the same shape as `an2-06`'s Abbott misprint and `an2-03`'s
  dropped hypothesis, and it is the first one caught by execution rather than by
  reading.** Recorded, not smoothed.
- **The paper the theorem is attributed to is not on disk.** Adams et al.,
  *Persistence images: a stable vector representation of persistent homology*,
  JMLR 18 (2017) — reference [4] in Dey and Wang. Bubenik, JMLR 16 (2015),
  reference [51], carrying Theorem 13.1 and Claim 13.1, is likewise absent. The
  lesson states both as attributions and depends on neither. **For the syllabus
  pass: this is the third and fourth paper in the same position, after Otter et
  al. (`lab-03`) and Singh–Mémoli–Carlsson (`lab-08`). Acquire all four or drop
  the citations.**
- **Gate 9 constrained content a second time, and again improved it.** The natural
  landscape demonstration is to perturb a diagram at random and report the ratio.
  Determinism forbids an unseeded perturbation, so the perturbed diagram is
  written out as literal coordinates — which is what made it possible to compute
  the **exact** landscape alongside the sampled one and discover that the library
  vector reports a ratio of 1.0684 where the theorem permits at most 1.
  *Correction, from the PR review:* a **seeded** perturbation is deterministic
  too and passes gate 9 perfectly well, so nothing here argues against seeded
  randomness — later units use it freely. What literal coordinates bought was
  **auditability**: the exact landscape could be computed by hand from numbers
  visible on the page, which is what turned an anomalous ratio into a diagnosis
  rather than a suspicion. Prefer literal values when a reader needs to
  re-derive the result independently; prefer a seed when the point is the
  behaviour of a sample.
- **A gate that does not exist yet: nobody checks that a lesson's canvas agrees
  with its caption.** Gate 7 is a manual read-back and it worked here — the three
  layer peaks were recovered at (295, 95), (258, 193) and (250, 264), matching
  half-lifetimes 1.5, 0.75 and 0.2, with the 2δ line found spanning x ∈ [70, 594]
  at y = 224–225. But it is manual, it is the only gate that is, and it is
  therefore the one most likely to be skipped under time pressure. Logged as a
  GATE-lane observation rather than acted on here.

**Source-boundary findings from `lab-07`, for the syllabus pass.**

- **The lab venv was silently broken, gate 9 passed on it for seven units, and
  the lockfile was innocent.** While drafting `lab-07`, `import sklearn.ensemble`
  raised `ModuleNotFoundError: No module named 'sklearn.ensemble._gradient_boosting'`;
  while drafting `lab-08`, `import igraph` raised the same shape
  (`No module named 'igraph._igraph'`), which takes `gtda.mapper` with it.
  Inspection: `sklearn/ensemble/` held its Python sources and **none** of its
  compiled extensions while 55 were present elsewhere in the package, and several
  `.dist-info` directories had no `RECORD` file — the signature of an incomplete
  install. Throughout, `importlib.metadata.version("scikit-learn")` returned
  `"1.3.2"` and **gate 9 printed `pins verified` on every one of `lab-01` to
  `lab-07`**. Running the lockfile's own documented rebuild,
  `uv pip install --reinstall -r resources/lab-requirements.txt`, repaired every
  package and **changed no version**: all nine pinned distributions re-resolved
  identically, and `lab-01` to `lab-06` re-passed gate 9 unchanged.
  **The lockfile was correct; the environment was not, and no check could tell
  them apart.** Gate 9 then caught the repair — `lab-07`'s `env` block failed
  because its recorded output had changed — which is the gate working, in the
  opposite direction, and the only watched failure of the whole episode.
  **The convention this produces: a lab `env` block imports the specific modules
  its later blocks use**, so that a broken subpackage surfaces as a gate-9 diff
  rather than as a green run. No change to `resources/lab-requirements.txt` is
  needed.

  **Resolved in the PR-comment pass, and promoted from convention to check.** A
  convention is the enforcement rung this project's failure record says does not
  hold on its own, and it did not hold here either: when the rule was made a
  static check in `scripts/check_lab_outputs.py` (every non-stdlib module a set
  imports must be imported by its `env` block, at the submodule it uses), it
  failed **eight of the nine sets** — including `lab-08` and `lab-09`, both
  written after the convention was adopted. All nine are retrofitted and
  re-recorded. Two false passes were closed while building it: distribution names
  passed to `version()` are not imports, and importing a parent does not declare
  a submodule. The check also fired on the author's own edit later in the same
  pass, when `sklearn.preprocessing` was added to `lab-07`'s leak block without
  being declared.

  **Escalated again in the second PR-comment pass, from module to name.** The
  module-level check still had a hole a reviewer found: `from sklearn import
  preprocessing` was recorded as a use of `sklearn`, so `import sklearn` in an
  `env` block certified it. The syntax is ambiguous — `from numpy import array`
  is identical and needs no submodule — and the resolution was to stop guessing:
  a name is covered either by a dotted import of `module.name` or by the `env`
  block running the same `from module import name`, which loads exactly what the
  later block loads whichever kind of name it is. **Six of the nine sets failed
  it**, and all nine are retrofitted, which makes each set's header claim ("API
  surfaces verified by execution: …") mechanical rather than self-attested — a
  renamed class now stops the set at its `env` block. Building it surfaced a
  false demand in the other direction as well, caught by its own negative
  control: `import gtda.homology` does load `gtda`, so a declared dotted path
  must declare its ancestors. Parent-vouches-for-child stays forbidden; child
  vouches for parent.
- **`lab-07` has the same missing-book finding as `lab-06`.** Its resource line
  names `giotto-tda: Pipeline API` and `scikit-learn Pipeline docs`. Dey and Wang
  **§13.3, printed 405–407**, is the only source in the whole programme that says
  what a *mean* of topological summaries is — Definition 13.10 and Theorem 13.10,
  plus the two statements on printed 407 that the Fréchet mean is not unique and
  that its computation "remains open". That is exactly the material the unit needs
  to justify why every pipeline averages in a vector space instead. **Add Dey and
  Wang §13.3 to `lab-07`'s line.**
- **The unit's first experiment failed, and the failure is the content.** The
  designed task — one circle against two — was won outright by a six-number
  baseline of coordinate means and covariances, 1.0000 against the topological
  pipeline's 0.9333, because the two generators differed in scale as well as in
  topology. The unit reports that, repairs the dataset by whitening each cloud,
  demonstrates the repair by showing the baseline features have spread **exactly**
  0.000000, and only then reports 0.9500. Recorded because the temptation to
  quietly fix the dataset and present only the final number is the exact
  self-attestation shape the observation log exists for.

**Source-boundary findings from `lab-08`, for the syllabus pass.**

- **`lab-08`'s mission strip is wrong on both halves of its second clause, and
  this one needs a syllabus edit rather than a rescoping in the lesson.** The
  strip reads: "Mapper's graph is a topological summary; its clustering leans on
  la-15's spectral machinery." First clause: true, and it needs an article fixed
  — Remark 9.1, printed 269, records that changing the cover turns Mapper into a
  merge tree (which cannot have a loop) or a relaxed Reeb graph (which can), so
  there is no "the" Mapper graph of a dataset. Second clause: **`la-15` is
  "Spectral theorem (real case)", sourced to Axler 7A–7B, and is unwritten** —
  only `la-01` and `la-02` exist on disk. And independently of that, the spectral
  theorem is not what a Mapper clusterer uses: the clusterer is a free choice with
  no counterpart in Definition 9.4, the executed one is DBSCAN (density-based, no
  eigenvectors), and even spectral *clustering* is a different construction and
  only one option among many. **For the syllabus pass: strike or rewrite the
  second clause.** This is the strongest strip finding since `top-07`'s, and
  unlike the others it cannot be repaired by bounding — the claim is false, not
  merely over-reaching.
- **Two implementations of Definition 9.4 disagree about b₁.** Same cloud, same
  lens, same clusterer, same nominal cover parameters: at 15 intervals and 45%
  overlap giotto-tda gives 28 nodes / 28 edges / b₁ = 1 and Kepler-Mapper gives
  26 / 25 / b₁ = 0; at 25 intervals / 30% they also differ. Three of five settings
  agree. **Neither has a bug** — Definition 9.4 leaves the interval placement,
  the meaning of "overlap fraction", the handling of boundary points and the
  treatment of empty clusters genuinely free. This is the module's first
  cross-check that fails, after `lab-03`'s agreement with gudhi and `lab-05`'s
  bound holding, and the reason it can fail without indicting anyone is that
  there is no uniqueness theorem for a Mapper graph the way there is for a
  barcode.
- **The two libraries' default overlaps give opposite answers on this data.**
  `gtda.mapper.CubicalCover` defaults to `overlap_frac=0.1`; `kmapper.Cover`
  defaults to `perc_overlap=0.5`. At 10 intervals the sweep gives b₁ = 0 at 0.05
  and 0.15 and b₁ = 1 at 0.30 and 0.45 — so out of the box giotto-tda is wrong
  here and Kepler-Mapper is right, silently, on identical input. Third instance of
  the "a default is part of the citation" discipline, after `lab-01`'s library
  defaults and `lab-02`'s three filtration conventions.
- **Theorem 9.16 does not apply to anything the unit computes, and the lesson says
  so at the point of use.** Of its four hypotheses — X path connected, f
  continuous, f well behaved, 𝒰 an open cover of a metric space — only the last
  holds non-vacuously on a 300-point cloud; continuity and well-behavedness are
  true for *every* f on a discrete space and therefore say nothing. The bridging
  statement Dey and Wang offer, that the discretized Reeb graph converges to the
  Reeb graph in the limit (printed 270), is **attributed to references [132] and
  [241], neither on disk**. Same shape as `lab-02`'s Rips/Čech split, and weaker:
  there the Nerve Theorem was in the source with checkable hypotheses.
- **Papers absent from disk, running total: eight.** Otter et al. (`lab-03`),
  Singh–Mémoli–Carlsson (`lab-08`, and cited in its syllabus resource line),
  Bubenik [51] and Adams et al. [4] (`lab-06`), and Dey's [231] and [291]
  (`lab-07`), plus [132] and [241] here — eight, counting the two this unit adds.
  Every one is recorded as an attribution at its point of use and nothing in the
  module depends on any of them. **For the syllabus pass: acquire or drop.**

**Source-boundary findings from `lab-09`, for the syllabus pass.**

- **The module's own threshold does not answer the module's own question, and the
  rule that does has no theorem.** On 600 points from a torus with noise sd 0.03,
  the six longest H₁ lifetimes are 1.2965, 1.2373, 0.5997, 0.5238, 0.5233, 0.4950
  and the answer is b₁ = 2. Four rules are applied. The **naive** one — 4 × the
  noise standard deviation, 0.1200 — admits all six and is the mistake the unit
  diagnoses. **lab-05's stability threshold**, at the *measured* displacement
  bound 4δ = 0.5248, admits three. The **coordinate-permutation null**, taken as
  the maximum over five pre-specified seeds, gives 0.5012 and admits five. The
  **largest-lifetime-gap heuristic** — which is `lab-01`'s ratio of 98.1,
  examined and explicitly not licensed by `lab-05` — gives 2, and gives 2 at four
  independent seeds. The unit reports all four and says plainly that the
  conclusion rests on the one with no theorem behind it. This is the honest end
  of the arc that began at `lab-01`, and it was not engineered: four thresholds
  were applied to see what they said.

  *Revised in the PR-comment pass.* The first two versions of this row recorded
  4δ = 0.12 and a single null draw of 0.4192, both of which the unit no longer
  contains: δ had been read off the Gaussian standard deviation, which is not a
  displacement bound (488 of the 600 points moved further than it), and one null
  draw is not a null distribution. Correcting them changed the unit's answers,
  which is why the row had to be rewritten rather than adjusted.
- **The stability rule is sound and not complete, and the object soundness is
  relative to is the repair.** Standard directions: *sound* — every bar the rule
  admits has a genuine partner in the **clean sample's** diagram, because a bar
  longer than 4δ is further than 2δ from the diagonal and no matching of cost
  **2δ** — lab-05's bound, not δ — can send it there; *not complete* — a real
  clean feature shorter than 4δ is **not guaranteed to be admitted** (not "is
  rejected": the rule is applied to the noisy bar, whose lifetime need not equal
  its clean partner's). The unit computes the clean diagram rather than asserting
  any of this: it has six long H₁ bars of its own (1.2963, 1.2843, 0.5425, 0.5175,
  0.5161, 0.5048), and each of the six longest noisy bars has a clean point within
  0.0488 against the bound 2δ = 0.2624 — six nearest neighbours rather than a
  bijection, so a consistency check the bound could have failed, with a factor of
  five to spare. The torus has b₁ = 2. So the extra bars belong to the clean
  600-point *sample*, not to the surface — sampling is not a displacement, and the
  theorem was never asked about it. Substituting a "sampling scale" for δ would
  make the rule bite and would silently convert Oudot's Corollary 3.6 into a
  heuristic wearing its clothes. Recorded because it is the most attractive wrong
  move in the unit.
- **A null answers a yes/no question far better than it supplies a number, and
  five draws are a variance probe rather than a test.** Permuting each coordinate
  independently preserves the sample size, the coordinate-wise marginals and the
  bounding box, and destroys the joint distribution — *not* "the same density":
  the torus concentrates on a surface and the permuted cloud is spread through the
  box, which is the whole difference the null is built to create. Its Rips
  filtration carries 1-cycles from 0.4006 to 0.5012 across five seeds — a 25%
  spread on a quantity used as a cutoff, which is why the *threshold* reading
  fails. As a **qualitative comparison** ("are bars 1 and 2 distinguishable from
  structureless data?", 1.2965 and 1.2373 against a maximum of 0.5012) the answer
  is the same at every seed. Call it that and not a test: **five draws cannot
  produce a p-value**, since an exact permutation test with five draws bounds the
  attainable p-value below at (0+1)/(5+1) = 0.167, which rejects nothing at any
  conventional level. Exceeding the largest of five sampled draws is exploratory
  evidence, not a controlled result; a capstone wanting a p-value needs hundreds
  of draws and must say so. Worth carrying regardless:
  the null is the only one of the unit's methods that transfers to real data
  unchanged, needing no known answer, no noise model and no second sample.
- **The unit's data is synthetic and the syllabus line says `self-directed
  dataset`.** Two reasons, both stated in the lesson: the gates forbid external
  requests and gate 9 forbids nondeterminism; and a known answer is what makes the
  three-threshold comparison possible at all. **For the syllabus pass: either
  relax the resource line to say "seeded synthetic dataset", or state that the
  self-directed dataset is the capstone's job and not `lab-09`'s.** The lesson
  marks each of its five conclusions survives / survives-with-assumption /
  does-not-transfer, so the boundary is documented either way.
- **Gate 9 caught a transcription slip in this very set.** The first draft of the
  `persistence` block's recorded output omitted its last two lines; gate 9 failed
  with a diff. Small, and worth recording as the third watched failure of the
  branch, after the wrapped-citation fix and the environment repair.

**Fifth review round — three claims that were true of the presentation and false
of the mathematics.**

All three were `P2`s from codex, all three were right, and all three had the same
shape: a correct observation about what a *picture* or a *table* makes easy, stated
as a fact about what the mathematics *requires* or *forces*. None was a slip in a
number. Recorded together because the shape is the finding.

- **`lab-02` attributed a coincidence to dimension, and the table's granularity is
  why it survived.** Problem 2(c) said the Čech and Rips counts "had to agree" at
  r = 0.15 and 0.25 because only 1-simplices are present, where the two definitions
  provably coincide. The coincidence part is real; the premise is not. Decomposing
  the recorded totals by dimension gives **8 edges + 1 triangle at r = 0.15 and 27
  + 18 at r = 0.25** — both radii already carry 2-simplices, so both were free to
  diverge and simply did not: every Rips triangle in this draw of twelve points also
  passes the Čech test, and at r = 0.35 four of the 91 stop. The edge rows are
  forced at every radius, r = 0.35 included; the triangle rows are geometry and this
  sample.

  **The `sandwich` block now prints per-dimension rows with an `all` row beneath**,
  so the totals 9 / 45 / 135 / 139 are still on the page and the decomposition that
  settles the question is visible rather than asserted in an answer key. This is the
  correction that matters: the claim was checkable against the block's own output
  and was not checkable against the block's own *table*, because the table had been
  aggregated past the distinction the question turns on. A recorded output is
  evidence only at the granularity it is printed at.

- **`lab-04` taught a false distinction between two bijective representations.**
  Problem 4(b) asked the reader to "name the specific downstream operation that
  requires the diagram rather than the barcode", and the answer named the bottleneck
  distance as having "no natural formulation on barcodes". It has one: match
  `[b, d)` to `[b′, d′)` at cost max(|b − b′|, |d − d′|) and let an unmatched bar
  of lifetime L cost L/2, which is exactly the distance from `(b, d)` to the
  diagonal. Two pictures of one list of intervals cannot differ in what is derivable
  from them. Prompt and answer rewritten around legibility, which is what the
  mission strip's "made visible" actually claims — a statement about the reader.

  The lesson's Self-check 2 had the error **in its keyed-correct answer**, and the
  distractor it marked wrong ("they carry identical information, so the choice is
  cosmetic") was the half-right one. Rekeyed: the diagram-requires-points option is
  now a distractor, the reviewer's own correction is the explanation, and the
  surviving wrong answer is wrong for the defensible reason (identical information
  does not make the choice cosmetic — it decides what a reader sees for free).

- **`lab-06` counted bars by counting landscape layers.** Problem 2(b) said the
  surviving features are "the layers that cross" the 2δ line, "readable at a
  glance". The number of layers crossing is the largest number of qualifying tents
  simultaneously positive at a **common** parameter — an overlap statistic and only
  a lower bound on the count. **Part (a)'s own disjoint diagram refutes it**:
  D = {(0,1), (10,11)} has two qualifying bars for any 2δ < 0.5 and exactly one
  layer that ever crosses. The two agree under nesting, which is the same hypothesis
  (a) is built to isolate — so the false claim in (b) was contradicted by the part
  immediately above it, in the same problem.

  What the landscape form does buy is stated instead: the test becomes
  ‖λ_D‖_∞ > 2δ, an inequality in the vector space Problem 3 works in, where the
  barcode form was a bar-by-bar pass. Existence is readable; cardinality is not.

**Two of the three were internally detectable**, by the branch's own standard: the
`lab-06` claim contradicts the part above it, and the `lab-02` claim contradicts a
decomposition of its own recorded numbers. That is the third time on this branch a
finding has been available from material already in the unit — after `lab-09`'s
factor of 2 (contradicted by its own table three blocks up) and `lab-06`'s missing
`persim.bottleneck` header entry. The pattern is not carelessness about sources; it
is that **no gate compares a unit's prose against the unit's own outputs**, and
reading for that is the reader's job at exactly the moment the reader is most
persuaded. Logged as a GATE-lane observation.

**Gates after the round:** lint 135/135 · 27 structural · 9 verbatim strips · gate 9
re-executing **59 blocks** with two runs agreeing on every set, `lab-02`'s changed
block included · 119 tests. Coverage passes on `lab-04` and `lab-06`; `lab-02`
reports `UNCHECKED — 0 refs`, which **predates this round** (confirmed against the
committed file) and is the `--min-refs` gap already recorded above for sets that
cite no numbered results. Gate 7 unaffected: no canvas script or `aria-label`
changed.

### lab — Computational Lab (executed Python; Edelsbrunner, Oudot, Dey and Wang)

| Unit | Title | Source | State |
|---|---|---|---|
| lab-01 | Setup and point clouds | gtda/numpy/scipy, executed; no book | ✓ |
| lab-02 | Simplicial complexes: Vietoris–Rips and Čech | Edelsbrunner **§III.2, printed 69–76** · gudhi, executed | ✓ |
| lab-03 | Computing persistence; boundary-matrix reduction | Edelsbrunner **§VII.1, printed 179–186** (with §IV.2, printed 101–106) · gudhi, executed | ✓ |
| lab-04 | Persistence diagrams and barcodes | Oudot **ch. 1, printed 13–28** · gudhi, executed | ✓ |
| lab-05 | Bottleneck and Wasserstein distance; stability | Oudot **ch. 3, printed 49–66** · persim, executed | ✓ |
| lab-06 | Vectorising diagrams: images, landscapes, Betti curves | Dey and Wang **§13.1, printed 390–395** · persim/gtda, executed | ✓ |
| lab-07 | TDA in machine-learning pipelines | Dey and Wang **§13.3, printed 405–407** · gtda/sklearn, executed | ✓ |
| lab-08 | Mapper | Dey and Wang **§9.3, printed 268–274** · gtda/kmapper, executed | ✓ |
| lab-09 | Mini-project: a full TDA analysis | the whole stack, executed; Dey **Thm 13.1** and Oudot **Cor 3.6** carried | ✓ |

---

## Mission-strip rescoping

Several `mission_link` strings claim more than their own sources establish. The
rubric requires the strip **verbatim** (gate 8), so quote it verbatim — then
teach only what the source supports and say so twice: a `.warn` block in the body
and a "Not established here" clause in the footer. Five of tda1's nine needed
this; tda2-07 is the model.

**Rescope in the open. Do not fill from recollection, and do not ship a
`NOT IN SOURCE:` marker either** — `lesson_lint.py` fails on the marker, and it
is a placeholder, not a resolution. **Do not soften the quotation to make it
true.**

Watch for it specifically at:

- **`top-07`**, whose strip asserts "Path components ARE what zeroth homology
  counts". Munkres §23–25 contains no homology at all, and the identification is
  false in general for arbitrary topological spaces — the strip's own hedge ("for
  TDA's finite complexes") is the part that is true and the part Munkres cannot
  supply. Bound it to what §25 proves about components and path components, and
  route the homology claim to `at1-07`.
- **`top-11`**, whose strip is about persistence diagrams and their limits, from
  a source (§31–32) with neither.
- **`top-05`**, whose strip reaches into multiparameter persistence.
- **`an2-01`**, whose strip — "The space of persistence diagrams is a metric
  space; fluency here is fluency there" — is the exact claim `tda1-05` established
  that *neither of its own sources defines*. `an2-01` teaches the axioms and the
  checklist; it does not get to assert the diagram-space metric, which is
  `tda1-06`'s.
- **`lab-03`**, whose strip says "reducing boundary matrices IS computing
  homology (aa-18 made runnable)" while `aa-18` is unwritten.
- **`lab-08`**, whose strip says its clustering "leans on la-15's spectral
  machinery". Wrong twice: `la-15` (Spectral theorem, real case, Axler 7A–7B) is
  **unwritten**, and the spectral theorem is not what a Mapper clusterer uses —
  the clusterer is a free choice with no counterpart in Definition 9.4, and the
  one this unit executes is DBSCAN, which computes no eigenvector of anything.
  This is the only strip finding in the module that **cannot be repaired by
  bounding the claim**, because there is no weaker true version of it: the
  dependency does not exist. It needs a syllabus edit, and the lesson meanwhile
  quotes the strip verbatim (gate 8) and refutes it in a `.warn` block. The
  tracker row is ✓ because the *unit* is complete under this branch's
  content-plus-gate scope; the syllabus edit is out of that scope and is listed
  here, which is where syllabus work is tracked.

**Never carry a theorem number across books.** Munkres, Hatcher and Lindström
number the same results differently; re-derive every number from the book being
cited. And **for every "every", "any", "no", "always", "never" in prose, run the
degenerate list** — empty, one point, finite, discrete, indiscrete, trivial group
— before writing the sentence. Across both CodeRabbit rounds on the S3 branch the
unhedged universal in explanatory prose was the single largest defect category,
and it cites nothing, so it passes every gate
(`2026-08-10-unhedged-universals-pass-every-gate`).

---

## Open decisions

### Resolved during the audit

- ~~**`an2` cites "Oxford M2 Metric Spaces notes", which does not exist.**~~
  **RESOLVED 2026-08-11.** Confirmed absent: the Oxford tree holds M2 Analysis I,
  II and III and no metric-spaces set anywhere; the whole tree has one file
  matching `*metric*` and it is an unrelated vectors refresher. Six `an2` units
  cited it, and `an2-04`'s only other source was nothing at all. **Stephen
  supplied Tom L. Lindström, *Spaces: An Introduction to Real Analysis*,** which
  is on disk with per-page markdown (384 pages) and closes every gap:

  | Gap found | Lindström section | Printed |
  |---|---|---|
  | metric topology, convergence, open/closed | 3.1–3.3 | 43–58 |
  | completeness, completion | 3.4, 3.7 | 59, 71 |
  | compactness, total boundedness, Arzelà–Ascoli | 3.5, 3.6, 4.8 | 63, 68, 107 |
  | **contraction mapping theorem** (found in *no* other on-disk source) | 3.4, 4.7, 4.9 | 61, 103, 112 |
  | uniform convergence of derivatives and integrals | 4.2–4.3 | 81, 86 |
  | polynomial approximation, Stone–Weierstrass | 4.10, 4.11 | 116, 123 |
  | **normed and Banach spaces** (found in *no* other on-disk source) | 5.1, 5.4 | 133, 150 |
  | spaces of bounded continuous functions | 4.5, 4.6 | 99, 101 |
  | **the derivative as a linear map** (found in *no* other on-disk source) | 6.1 | 174 |

  **Registered in `resources/bookmap.json` as `"Lindstrom"` in its own commit,
  ahead of any `an2` content** — same handling as Spivak in S3. The key is ASCII
  to match the on-disk folder name; the `title` field carries "Lindström". Note
  the folder name does **not** end in `.pdf`, unlike every other entry, so the
  `md`/`pages` paths differ in shape from their neighbours; this is correct, not
  a typo. `pull.py` reaches it and `validate_syllabus.py` still reports
  `syllabus OK`.

  **For the syllabus pass:** the `resources` strings still name the Oxford notes.
  Re-pointing them is a syllabus edit and this branch is content-only, so `an2`
  is authored from Lindström and the divergence is recorded here — the same
  handling S3 gave `cat-05`.

- ~~**`lab`'s sources and artifact shape.**~~ **RESOLVED 2026-08-11 by Stephen.**
  Install the libraries and source from them plus the books; problem sets carry
  runnable Python, executed, with real output recorded. Environment and pinned
  versions are in the source map above. Consequences worth naming: gate 1 goes
  vacuous on a set that cites no numbered results (use `--min-refs`), and rubric
  item 1.12's "book + section + PDF page(s), resolved via `bookmap.json`" has no
  reading for a library call — `lab` cites version + symbol instead, which is a
  rubric extension recorded here rather than a rubric edit made here.

### Still open

- **`an2-09`'s cited source is the wrong subject, and the error is invisible at
  the granularity anyone checks.** The unit's `resources` say "Oxford M2 notes
  (multivariable analysis)", and the handover identified this as
  `M5 Multivariable Calculus\lectures23.pdf`. That file exists, and it is **vector
  calculus** — multiple integrals, Jacobians for cylindrical and spherical
  polars, div and curl, the divergence theorem, Stokes, Gauss. It contains
  **zero occurrences of "linear map" and zero of "best linear"**. `an2-09`'s hook
  is "The derivative is not a number but a linear map — the best linear
  approximation, made rigorous", which that text never does. The accompanying
  `Online Lecture Notes-20220531.zip` holds the previous year's edition of the
  same lectures and adds nothing. Authored from Lindström ch. 6 instead.
  **For the syllabus pass: re-point `an2-09`.**

- **`an2-06`'s hook needs a source outside its cited range, and both candidate
  sources set the result as an exercise.** The hook promises "approximate ANY
  continuous function by polynomials". Abbott's Weierstrass Approximation Theorem
  is at printed **242**, in §8.3 (Fourier), reached through the Fejér kernel as
  **Exercise 8.3.10** — not in the cited 6.4–6.7 range. Lindström has it properly
  in §4.10 ("Polynomials are dense in the continuous functions", printed 116) and
  §4.11 (Stone–Weierstrass, printed 123), which is where the unit will take it.
  Separately, the M-test: **Abbott states it** (printed 168, in the cited §6.4),
  while **Lindström sets it as Exercise 11 of §4.2** (printed 85). So the unit
  takes the M-test from Abbott and the approximation theorem from Lindström, and
  says which does which. **For the syllabus pass: `an2-06`'s Abbott range should
  reach 8.3, or the hook should drop the approximation clause.**

- **`an2-05`'s citation is right about the book and wrong about the volume — but
  only just.** The unit cites "Oxford M2 Analysis III notes", and Analysis III
  *is* the right volume: chapter 5, "Limits and the integral", is §5.1
  Interchanging the order of limits and integration, §5.2 Interchanging limits
  and differentiation, §5.3 Power series. That is exactly the unit's subject. The
  trap is that the folder holds **two** Analysis III artifacts, and only one is
  the notes: `AnalysisIII-2022-notes.pdf` (Lackenby, after Green) has chapter 5;
  `Analysis3LecturesTT2022.pdf` is a slide deck with incremental overlays, so
  every frame repeats and nothing has a stable page to cite. The four units
  citing "Analysis III notes" for **normed spaces** (`an2-07`) and **C[a,b]**
  (`an2-08`) are a different matter: Analysis III is integration and contains
  neither — grep returns zero for "normed", "Banach" and "metric space" across
  all three Oxford analysis volumes. **For the syllabus pass: `an2-07` and
  `an2-08` should not cite Analysis III.**

- **The Oxford notes cannot be read the way the handover said.** The instruction
  was to read them with the Read tool's `pages` parameter; that renders via
  `pdftoppm`, which is not installed here, and the call fails outright. What works
  is `pdftotext` (present) plus `pypdf`/`pdfplumber`. Not blocking — `an2` no
  longer needs the Oxford PDFs — but recorded because the source map now carries
  an extraction-method column for exactly this reason
  (`2026-08-11-handover-prescribed-an-unexecuted-tool-path`).

- **Two of `lab`'s three named papers are not on disk.** `lab-03` cites Otter et
  al., *A roadmap for computing persistent homology*, and `lab-08` cites
  Singh–Mémoli–Carlsson, *Mapper* (2007). Neither is anywhere under the corpus
  root, and both are registered in `resource_sources.json` only as name prefixes,
  so `validate_syllabus.py` passes on them. Both units have a real primary source
  without them (Edelsbrunner VII; the executed Mapper implementations), so neither
  blocks. **For the syllabus pass: acquire both, or drop the citations.**

- **`validate_syllabus.py` cannot see any of this.** `resource_sources.json`
  registers **name prefixes** — "Oxford", "giotto-tda", "GUDHI", "self-directed"
  and so on — so a citation resolves whenever its first word is registered,
  whatever the rest of the string says and whether or not anything on disk backs
  it. Every finding above passed the validator. **Treat a green validator as no
  evidence about S2 sources at all.** Covered by open observation
  `2026-08-09-syllabus-resources-resolve-to-nothing`.

- **`gate.py` and `mission.py` still live in a session scratchpad.** Two gates of
  the eight are carried between programmes by hand-copying, which is how
  `mission.py`'s negative control came to be broken without anyone noticing, and
  `gate.py` has escaped the same fate only by luck. Promotion into `scripts/`,
  with the controls as repo fixtures under `tests/`, has now been deferred three
  times. **Proposed: promote both in their own commit, out of scope for this
  content branch**, per `2026-08-10-gates-check-syntax-never-sense`.

- **The `lab` environment is pinned in prose, not in a lockfile.** The install
  commands above now carry `==` pins on all seven packages and an exact Python,
  so a rebuild that follows them reproduces the recorded outputs. What no gate
  can catch is a rebuild that *does not* follow them: nothing in the repo asserts
  the environment a `lab` output came from, and the outputs are the worked
  answers. A committed `requirements-lab.txt` (or `uv.lock`) with a check that
  each `lab` problem set names the environment it ran in would close that, and
  it is a repo-structure change — **out of scope for this content-only branch;
  decide it at the `lab` PR.** Raised by CodeRabbit on PR #17.

- **The Expository Writing folder is still not retrievable.** Not an S2 problem —
  no S2 unit names it — but it will block `pw-05` when S1 is authored, and S4's
  `cap-04` already substituted `curriculum/LESSON-RUBRIC.md` for it. Carried
  forward from S3 so it is not lost between programmes.
