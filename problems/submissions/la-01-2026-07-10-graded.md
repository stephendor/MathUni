# la-01 — Graded submission (2026-07-10)

Rubric source: problems/solutions/la-01.md. Marking stance: skeptic first,
logic judged on its own merits, hand-waves named as hand-waves.

**Note on scope, addressed first per your flag:** Problem 5 is excluded
from scoring — see the note after Problem 5 below. Your objection is
correct and there's a real curriculum bug behind it.

---

## Problem 1 — solve for x — **0.55 / 1.0**

- Isolating 2x componentwise — mostly correct, but **there's a sign error**:
  in component 3, 1+2x=−6 gives 2x=−7, so x=−7/2 — you wrote x=**7/2**
  (dropped the negative sign). Components 1, 2, 4 are correct
  (x=1/2, 6, 1/2). — **0.3/0.4**.
- Dividing to find x — same error carries through; three of four components
  correct — **0.25/0.4**.
- Verification / stating the final answer clearly — you never assembled the
  final vector x=(1/2, 6, −7/2, 1/2) as a single answer, and there's no
  substitution check — **0/0.2**.

**Sign errors under time pressure are exactly the kind of slip a
verification step catches** — plugging your answer back into the original
equation would have caught the −7/2 vs 7/2 flip immediately.

## Problem 2 — −(−v) = v — **0.85 / 1.0**

This is a genuinely valid *alternative* proof to the rubric's — you don't
invoke the general uniqueness-of-inverses theorem; instead you add −(−v)
to both sides of v+(−v) = −(−v)+(−v) and simplify directly. Checking it:
LHS reduces to v+0=v (using (−v)+(−(−v))=0 by definition), RHS reduces to
0+(−(−v))=−(−v). That's fully correct and complete.

- Identifies v+(−v)=0 as exhibiting v as an inverse of −v — implicit in
  your setup, present but not narrated — **0.3/0.35**.
- Alternative to "prove uniqueness": your direct cancellation does the
  same job rigorously, but "simplifying both sides" compresses two
  separate definitional facts ((−v)+(−(−v))=0 and 0+(−(−v))=−(−v))
  into one word — **0.3/0.35**.
- Explicit final inference — **0.25/0.3**, clean.

No penalty for using a different (valid) technique than the model
solution — this is correct logic, just under-narrated at the "simplifying"
step, the same pattern flagged in this morning's pw-01 grading.

## Problem 3 — av=0 ⇒ a=0 or v=0 — **0.65 / 1.0**

- a=0 case — **0.15/0.15**, correctly disposed.
- Multiplies by 1/a — **0.3/0.3**, correct.
- Associativity computation — you write v=1v=(1/a)(av) as one chain
  without showing the intermediate ((1/a)·a)v = 1v step explicitly — the
  claim is true but the associativity move itself isn't demonstrated,
  just asserted — **0.2/0.3**.
- Justifying (1/a)·0 = 0 — this is **asserted, not justified** (rubric
  explicitly calls this out: it needs the same style of argument as
  Theorem 1.29's 0v=0, not just written down) — **0/0.25**.

## Problem 4 — empty set not a vector space — **0.7 / 1.0**

- Identifies the additive-identity clause as the failure point —
  **0.4/0.4**, correct.
- Argument that ∅ has no element for 0 — correct but brief ("it contains
  no elements") — **0.3/0.4**.
- Notes why the *other* seven clauses are vacuously satisfied, explaining
  why this is the *only* failure point — **not addressed at all** —
  **0/0.2**.

## Problem 5 — 𝔽⁴ = U ⊕ W — **excluded from scoring**

**You're right, and this is a real problem-set bug, not a judgment call.**
I checked lessons/la/la-01.html directly: its footer explicitly states
*"Deferred to la-02: subspaces, sums of subspaces, and direct sums"* — the
lesson body contains zero mention of subspaces, sums, or direct sums. The
problem set's own annotation on Problem 5 claims it was "previewed at the
end of la-01," which is simply false against the actual lesson file. This
problem requires the subspace test (Theorem 1.34) and the two-subspace
direct-sum test (Theorem 1.45) — both la-02 content, which is currently
`locked` in your progress state. Refusing to attempt it with un-taught
machinery was the correct call, not an evasion.

**Action taken:** flagging problems/sets/la-01.md Problem 5 for a fix —
it belongs in la-02's problem set, not la-01's. Not fixing the file
mid-session since it needs a deliberate edit (retire from la-01, ensure
la-02's set covers it or an equivalent), logged for follow-up.

## Problem 6 — extended reals, associativity — **0.95 / 1.0**

Nice alternative counterexample to the model solution — instead of the
triple (∞,∞,−∞), you used (t,∞,−∞) for real t: (t+∞)+(−∞) = ∞+(−∞) = 0,
but t+(∞+(−∞)) = t+0 = t. Since t≠0 in general, these disagree. Cleaner
than the model's, since it only needs t+∞=∞ and ∞+(−∞)=0, not ∞+∞=∞.

- Identifies associativity as the axiom to test — **0.2/0.2**.
- First bracketing (t+∞)+(−∞)=0 — **0.3/0.3**, correct.
- Second bracketing t+(∞+(−∞))=t — **0.3/0.3**, correct.
- Explicit contradiction + conclusion — correct, but you never state
  t≠0 — if t=0 the two sides coincide (0=0) and there's no contradiction;
  the proof needs "for any t≠0 (e.g. t=1)" to be airtight — **0.15/0.2**.

On the exposition critique: fair — the problem set's phrasing compresses
Axler's rule statement more than it should. Noted below, separate from
your score.

---

## Total: (0.55+0.85+0.65+0.7+0.95)/5 = **0.74 / 1.0** (Problem 5 excluded)

**Below the 0.8 mastery threshold — building a remedial set (3 problems)
targeting the specific gaps below, not a full redo.**

## What was genuinely good

- Problem 2's alternative proof of −(−v)=v is fully valid, self-contained
  reasoning — not just reproducing the expected technique.
- Problem 6's counterexample is arguably cleaner than the model solution.
- Correctly caught a real curriculum bug in Problem 5 rather than forcing
  an answer with tools you don't have yet — good instinct, worth trusting
  going forward.

## The gap that matters most

**Asserting definitional facts instead of demonstrating them** — (1/a)·0=0
in Problem 3, the associativity step in Problem 3, and the "why do the
other axioms hold vacuously" half of Problem 4 are all cases where a true
statement is written down without the one-line justification the rubric
is actually testing. Same family as this morning's pw-01 pattern (naming
the logical move) but here it shows up as *skipping small lemma-style
facts* rather than skipping technique names. Also: Problem 1's sign slip
is a concrete reminder that a 10-second substitution check pays for itself.
